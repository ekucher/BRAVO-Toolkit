# Конфіг gitleaks: розширює стандартний набір правил.
#
# Єдине відхилення від дефолту — .gitleaksignore виключений зі скану.
# Причина (виявлено на PR #60): рядок fingerprint-а має форму
# <40-hex-sha>:<file>:generic-api-key:<line> — 40-символьний hex поруч зі
# словом "api-key" сам збігається з entropy-евристикою generic-api-key,
# тобто КОЖЕН доданий запис-виняток породжує нову знахідку у файлі
# винятків (рекурсія). .gitleaksignore за визначенням містить лише
# fingerprint-и вже підтверджених false positive, не секрети.
[extend]
useDefault = true

[allowlist]
description = ".gitleaksignore містить лише fingerprint-и, не секрети"
paths = ['''\.gitleaksignore$''']
