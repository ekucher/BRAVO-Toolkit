# 5.3.0 — вільне місце: health-сигнал, звітність рішення і симетрія Archive/Maintenance

Статус: **специфікація, до реалізації**. Пункти 1-2 — залишковий борг з
`docs/BRAVO_523_RC6_ACCEPTANCE_EVIDENCE_20260913.md` §8. Пункти 3-4 знайдено
під час real-server acceptance 5.2.4-rc.1 на `LIMS-TOP` 13.09.2026.

> **Що вже зроблено іншими релізами.** Первісна редакція цього документа
> охоплювала повернення below-floor relaxation і peak-safe оцінку. Це
> реалізовано в **5.2.4** (`hotfix/5.2.4`): `RequirementPolicy` для Archive
> переведено на `ArchivePeakSafe`, а вимогу підперто доведеною верхньою
> межею з нестиснутого розміру джерела. Жоден із пунктів нижче 5.2.4 не
> торкається — три з них вона робить помітнішими, а один уперше проявила.
>
> **Передумова.** 5.2.4 має бути промотована у stable і синхронізована в
> `developer` за `RELEASE_POLICY.md` §12.2 до початку цієї роботи. Усі
> чотири пункти нижче лежать поруч зі зміненим у 5.2.4 кодом, і робити їх
> на несинхронізованій базі означає гарантований конфлікт злиття.

Спільна теза: після 5.2.4 `MinimumFreeSpaceGB` — це **сигнал здоров'я
тому**, а не гейт операції. Сигнал має бути досяжним (§1), видимим у
підсумку (§2), правдиво атрибутованим (§3) і однаково трактованим усіма
операціями (§4). Зараз він не є жодним із чотирьох.

---

## 1. Том, менший за поріг, попереджає вічно

### Проблема

Health-гілка порівнює вільне місце з абсолютним порогом:

```powershell
} elseif ($null -ne $minimumGB -and $healthCapacityState -eq 'Known' -and
          $null -ne $healthAvailableGB -and [double]$healthAvailableGB -lt [double]$minimumGB) {
    $healthStatus = 'Warning'
    $healthReason = 'BelowHealthFloorNoFreeSpaceRequirement'
}
```

`modules/BRAVO.DiskSpace/BRAVO.DiskSpace.psm1`, крок 7.

Якщо **загальна ємність** тому менша за поріг, умова істинна завжди,
скільки місця не звільняй. Попередження не можна закрити дією оператора —
воно перестає бути сигналом і стає шумом.

Приклад із парку (`LIMS-TOP`, acceptance 5.2.3): `G:` має 15 GB загальної
ємності при порозі 20 GB. Кожен прогін давав
`BelowHealthFloorNoFreeSpaceRequirement`, `exit 10` і сповіщення в ALERTS.

### Поточний обхід і чому він поганий

Том вноситься в `Maintenance.Limits.ExcludedDrives`. Це прибирає шум, але
разом із ним прибирає **весь** health-моніторинг тому: заповнений на 100%
`G:` після цього мовчить так само, як порожній. Обмін неправильний — ми
глушимо датчик, бо він відкалібрований не під цей том.

### Пропозиція

Коли `TotalGB < MinimumFreeSpaceGB`, абсолютний поріг для цього тому
недосяжний за побудовою. Замінити його на відносний — частку ємності:

```
effectiveFloor(volume) =
    if TotalGB >= MinimumFreeSpaceGB : MinimumFreeSpaceGB
    else                             : TotalGB * SmallVolumeFloorPercent / 100
```

`SmallVolumeFloorPercent` — параметр із значенням за замовчуванням (10%
як стартова пропозиція; остаточне число — рішення власника). Для `G:` це
дає 1.5 GB замість 20 GB: досяжно, і датчик далі працює.

Вимоги до реалізації:

- **окрема `Reason`** (наприклад `BelowDegradedHealthFloorSmallVolume`),
  щоб у журналі було видно, що поріг вироджений, і яким саме він став —
  інакше оператор бачитиме попередження про 1.5 GB і не розумітиме, звідки
  це число при налаштованих 20;
- `TotalGB` уже доступний у результаті з 5.2.3 (`New-BRAVODiskSpaceResult`),
  додаткових вимірювань не потрібно;
- вироджений поріг застосовується **тільки** до health-оцінки. Том, який
  операційно потрібен і на якому бракує місця, блокується як і раніше —
  ця гілка не змінюється;
- `ExcludedDrives` зберігає нинішню семантику повністю.

### Критерії приймання

1. Том з `TotalGB` меншим за поріг і достатнім вільним місцем відносно
   виродженого порогу — **Success**, без попередження.
2. Той самий том, заповнений понад вироджений поріг — **Warning** з новою
   `Reason`, у тексті видно фактичний поріг.
3. Том з `TotalGB` більшим за поріг — поведінка **не змінилась** (наявні
   серії `DiskSpace/S*` лишаються зеленими без правок).
4. На `LIMS-TOP` `G:` виведено з `ExcludedDrives` і не дає попереджень,
   лишаючись під моніторингом.

---

## 2. Лічильник `Попереджень` не бачить попереджень preflight

### Проблема

У підсумку Maintenance значення береться з
`$script:BRAVOMaintenanceStepWarnCount`
(`modules/BRAVO.Maintenance/BRAVO.Maintenance.Runtime.ps1:10437`), а цей
лічильник інкрементує **лише** `Add-BRAVOMaintenanceStepOutcome`
(рядок 1317).

`Invoke-BRAVOMaintenanceDiskSpaceCheck` своїх попереджень туди не подає —
вона друкує їх напряму:

```powershell
foreach ($warningText in @($classified.Warnings)) {
    Write-Log $warningText -Level 'WARNING'
}
```

Тобто попередження є в журналі й впливає на код завершення, але в підсумку
його немає. Оператор бачить `Попереджень: 0` поруч із `exit 10` — підсумок
суперечить сам собі. Виявлено на rc.3 у циклі 5.2.3.

### Чому це стало важливішим після 5.2.4

До 5.2.4 below-floor на required томі **блокував**, тож до підсумку справа
не доходила взагалі. Після 5.2.4 той самий стан дає WARNING і прогін
триває — тобто розбіжність лічильника з кодом завершення тепер
траплятиметься регулярно, а не як рідкісний випадок.

### Пропозиція

Перевірка місця — не пронумерований крок, тож нумерацію `[N/Total]` чіпати
не можна (`Add-BRAVOMaintenanceStepOutcome` навмисно не рухає
`$script:BRAVOMaintenanceStepCurrent` — коментар на рядку 1303).
`Add-BRAVOMaintenanceStepOutcome` саме для цього й призначена: вона
оновлює лічильники й журнал кроків, не торкаючись нумерації.

Тому: коли `@($classified.Warnings).Count -gt 0`, викликати її один раз зі
`Status = 'WARN'` і назвою на кшталт «Перевірка вільного місця», а деталі
скласти з переліку попереджень.

Один виклик на весь набір, не по одному на попередження — інакше підсумок
роздується на серверах із багатьма томами.

Окремо перевірити симетричний випадок: чи потрапляє в лічильники
блокувальна відмова цієї ж перевірки (`return $false` → `exit 60`), чи там
та сама діра з `Помилок: 0`.

### Критерії приймання

1. Прогін Maintenance із below-floor попередженням: `Попереджень` ≥ 1,
   код завершення 10, підсумок і код узгоджені.
2. Прогін без попереджень: `Попереджень: 0`, код 0 — без змін.
3. Нумерація `[N/Total]` пронумерованих кроків не зсунулась.
4. Блокувальна відмова перевірки місця відображена в `Помилок`.

---

## 3. Зведений рядок рішення приписує шляхам чужу причину

### Проблема

`Resolve-BRAVODiskSpaceGroupDecision` ухвалює рішення на групу
`CapacityKey`, але операторський рядок формує
`Group-BRAVODiskSpaceMessagesByCapacityKey`
(`modules/BRAVO.DiskSpace/BRAVO.DiskSpace.psm1:883`), яка групує **лише за
`CapacityKey`**, а `Reason` бере від першої entity й більше не переглядає:

```powershell
$key = if ([string]::IsNullOrWhiteSpace([string]$entity.CapacityKey)) {
    "path:$($entity.DisplayPath)"
} else {
    "key:$($entity.CapacityKey)"
}
if (-not $groups.Contains($key)) {
    $groups[$key] = [pscustomobject]@{ Reason = $entity.Reason; Paths = ... }
}
[void]$groups[$key].Paths.Add([string]$entity.DisplayPath)
```

Рядки 894-902. Шляхи додаються всі, причина лишається від першого.

Entity з `RequiresFreeSpace=false` вирішуються окремо у фазі 1 (крок 7,
рядок ~610) і не входять у групу вимоги — але в **повідомлення** входять,
бо `CapacityKey` у них той самий. Коли на одному томі зійшлися health-only
entity і destination із різними причинами, усі отримують причину першої.

### Доказ

`BRAVO_ARCHIV` 5.2.4-rc.1 на `LIMS-TOP`, 13.09.2026 17:01, поріг 1000 GB.
Структурований лог рішення (`Write-BRAVODiskSpaceDecisionLog`):

```
DisplayPath=D:\                      Status=Warning Blocks=False Reason=BelowHealthFloorNoFreeSpaceRequirement
DisplayPath=D:\LIMS\ARCHIV\MODEL     Status=Warning Blocks=False Reason=BelowHealthFloorButRequirementSatisfied
DisplayPath=D:\LIMS\ARCHIV\BLOG      Status=Warning Blocks=False Reason=BelowHealthFloorButRequirementSatisfied
DisplayPath=D:\LIMS\ARCHIV\BRAVOEXCH Status=Warning Blocks=False Reason=BelowHealthFloorButRequirementSatisfied
```

Зведений рядок у тому самому прогоні:

```
D:\, D:\LIMS\ARCHIV\MODEL, D:\LIMS\ARCHIV\BLOG, D:\LIMS\ARCHIV\BRAVOEXCH: BelowHealthFloorNoFreeSpaceRequirement
```

Трьом із чотирьох шляхів приписано протилежне за змістом: «вимоги немає»
замість «вимога є і задоволена».

Дефект успадкований з 5.2.3 (функція з'явилася в `236e559`, присутня в
`origin/master` без змін; 5.2.4 її не торкалася). До 5.2.4 він був
непомітний: при порозі вище за вільне місце всі destination блокувалися з
`BelowFloorEstimateNotPeakSafe`, тобто причина в групі збігалася. 5.2.4
вперше створює на одному томі **змішані причини**.

Та сама функція обслуговує `Problems` (рядок 878), тож при блокуванні з
різними причинами на одному томі оператор так само побачить одну.

### Пропозиція

Додати `Reason` у ключ групування. Намір §51.1 зберігається повністю —
«спільна умова повідомляється раз на capacity resource», а спільна умова
це саме однакова причина. Очікуваний вивід для прикладу вище:

```
D:\: BelowHealthFloorNoFreeSpaceRequirement
D:\LIMS\ARCHIV\MODEL, D:\LIMS\ARCHIV\BLOG, D:\LIMS\ARCHIV\BRAVOEXCH: BelowHealthFloorButRequirementSatisfied
```

Дедуплікація не втрачається: шляхи з однаковою причиною на одному томі
далі йдуть одним рядком.

### Критерії приймання

1. Том з кількома entity **однієї** причини — один рядок, як зараз
   (без регресії дедуплікації).
2. Том з entity **різних** причин — окремий рядок на кожну причину,
   кожен шлях лише у своєму.
3. Жоден шлях не з'являється в рядку з причиною, відмінною від його
   `Reason` у структурованому лозі.
4. Те саме для `Problems`, не лише для `Warnings`.
5. Entity без `CapacityKey` далі групуються по `DisplayPath` окремими
   повідомленнями.

---

## 4. Maintenance трактує поріг інакше за Archive — на тому самому ключі

### Проблема

`Get-BRAVOMaintenanceDiskSpaceEntities`
(`modules/BRAVO.Maintenance/BRAVO.Maintenance.Runtime.ps1:6650-6658`)
створює єдину write-required entity (`$ROOT_LIMS`) з жорстко заданим
`RequirementGranularity = 'Unknown'` (рядок 6655), бо в Maintenance немає
аналога `Get-BRAVOArchiveEstimatedSpaceRequirement`. Коментар у коді
називає це чесно.

Наслідок: `GroupRequirementState` ніколи не стає `Known`, гілка з
`RequirementPolicy` (рядки 751-763) недосяжна, а `MaintenanceExactOnly`
(рядок 6703) — **мертве значення**. Виконується завжди fallback:

```powershell
$lowerBound = 0.0            # оцінки немає
$residual = $available
if ($residual -lt $floor) {
    $groupStatus = 'Error'; $groupBlocks = $true
    $groupReason = 'BelowFallbackFloorNoEstimate'
}
```

Рядки 774-786. Тобто для Maintenance поріг лишився **жорстким гейтом**,
яким він був у 5.2.3.

Обидві операції читають **той самий ключ**:
`maintenanceSettings.Limits.MinimumFreeSpaceGB`
(`BRAVO.Maintenance.Runtime.ps1:335`, `BRAVO.Archive.Runtime.ps1:135`).

### Доказ

`LIMS-TOP`, 13.09.2026, том `D:` — 931.51 GB ємності, ~715 GB вільних,
фактична потреба архівації 0.0686 GB:

| Поріг | `BRAVO_ARCHIV` | `BRAVO_MAINTENANCE` |
|---|---|---|
| 20 GB | Success, exit 0 | Success |
| 730 GB | Warning `BelowHealthFloorButRequirementSatisfied`, архіви 3/3, exit 10 | не прогонялось |
| 800 GB | не прогонялось | `BelowFallbackFloorNoEstimate`, exit 60, 2 сек (17:41) |
| 1000 GB | Success, архіви 3/3, SFTP 7/7, exit 0 (17:01) | `BelowFallbackFloorNoEstimate`, exit 60, 2 сек (17:07) |

Обслуговування завершується за дві секунди, не виконавши жодного кроку, і
надсилає критичне сповіщення.

### Чому це операційно небезпечно

Після 5.2.4 оператор, який підняв поріг заради толерантності архівації,
**тихо ламає обслуговування**. Архівація проходить і рапортує успіх;
Maintenance падає — але зазвичай уночі, за розкладом, і зв'язок із правкою
конфігурації не очевидний. Дві операції з одного комплекту дають
протилежні відповіді на те саме питання про той самий том.

### Варіанти рішення

Рішення про підхід — за власником; нижче компроміси, не рекомендація до
негайної реалізації.

1. **Дати Maintenance власну оцінку потреби.** Найближче за духом до 5.2.4
   і робить `MaintenanceExactOnly` живим. Найдорожче: потрібен
   консервативний верхній кордон для ротації журналів і очищення.
2. **Дозволити below-floor як WARNING і без оцінки** — для
   `MaintenanceWorkingVolume`, з окремою `Reason`. Дешево, але послаблює
   єдиний наявний захист робочого тому; вимагає окремого обґрунтування,
   бо суперечить fail-closed за замовчуванням.
3. **Розділити ключі** (`Limits.MinimumFreeSpaceGB` для health,
   окремий для Maintenance-гейта). Знімає приховану зв'язність, але додає
   конфігураційний ключ і міграційний борг.
4. **Лишити як є, задокументувавши.** Прийнятно, лише якщо поріг у парку
   тримається реалістичним; тоді потрібна явна перевірка при розкатці.

Незалежно від вибору: розбіжність має бути **видимою**. Мінімум —
діагностика, коли `MinimumFreeSpaceGB` перевищує ємність тому, до якого
застосовується (на `LIMS-TOP` поріг 1000 GB стояв на томі 931.51 GB, і
жоден рядок цього не назвав).

### Варіант 1 розгорнуто: алгоритм оцінки потреби

Проєкт, не рішення. Наведений, щоб варіант 1 можна було оцінити за
вартістю, а не за загальним описом.

#### Контракт

Той самий, що в Archive: повернути для кожного `CapacityKey` або
**доведену верхню межу**, або чесне `Unknown`. Не прогноз — межу, яку
операція фізично не може перевищити. Пряме перенесення принципу 5.2.4
(`sourceBytes × (1 + 2%)` як стеля).

`BRAVO.DiskSpace` при цьому **не змінюється зовсім**. Змінюється лише те,
що Maintenance у нього подає: `RequirementGranularity = 'Entity'` і
заповнений `RequiredGB` замість нинішнього `'Unknown'`
(`BRAVO.Maintenance.Runtime.ps1:6655`). Гілка `MaintenanceExactOnly`
(рядки 751-763 у `BRAVO.DiskSpace.psm1`) стає досяжною сама собою.

#### Терми за операціями

**1. Ротація журналів** — `Invoke-BRAVOLogRotation` (рядок 3161) через
`Move-BRAVOLogWithSequence` (рядок 2759), яка виконує `Move-Item`
(рядок 2924).

- Той самий том (Trace: `<LIMSRoot>\TraceSRV.out` →
  `<LIMSRoot>\ARCHIV\LOGS\Trace\<дата>`) — перейменування, дельта **0**.
- Крос-томовий (Apache `C:\Br-a-vo.web\apache\logs` → `D:\...`,
  BravoWeb `C:\Br-a-vo.web\www\log` → `D:\...`) — `Move-Item`
  вироджується в copy+delete. На томі призначення дельта дорівнює
  **сумі розмірів файлів джерела**, і вона постійна, не транзієнтна.

Списки файлів формуються до запису: `Get-BRAVOExchangeApiLogFiles`
(3012), `Get-BRAVOApacheLogFiles` (3064), `Get-BRAVOWebApplicationLogFiles`
(3081). Термін точний, не оцінковий.

Ротацією внесок Trace і exchangAPI **не вичерпується**: обидва далі
пакуються в добові архіви — це терм 2.

**2. Добові архіви Trace і exchangAPI** — `Update-BRAVOTraceDailyArchive`
(рядок 3831). Найтонше місце.

Обидва компоненти йдуть через **один движок**: `exchangAPI` викликає
`Invoke-BRAVOTraceArchiveMaintenance` з `-ComponentLabel 'exchangAPI'` і
`-ArchiveNamePrefix 'exchangAPI'` (рядок 8578). Терм має покривати обидва,
у різних каталогах того самого тому (`…\ARCHIV\LOGS\Trace` і
`…\ARCHIV\LOGS\exchangAPI`).

Оновлення накопичувальне й транзакційне: **копіює наявний архів** у
`.work`, дописує нові файли, публікує атомарною заміною. `.work` навмисно
лежить усередині `Trace\` заради `[IO.File]::Replace` на тому самому томі
(`New-BRAVOTraceWorkArchivePath`, рядок 3705). Отже в піку на томі лежать
**дві копії** добового архіву:

```
peak(компонент, група дати) = розмір_наявного_архіву + Σ(розміри_нових_файлів)
```

Другий доданок — стеля: стиснені entries не можуть перевищити нестиснуте
джерело.

Підтверджено прогоном на `LIMS-TOP` 13.09.2026 17:48: обидва компоненти
створили транзакційний partial у `.work`
(`Trace_20260913.92626af2….partial.mdz`,
`exchangAPI_20260913.b7122883….partial.mdz`) і опублікували результат як
`UPDATED` — тобто наявний архів справді копіюється, а не створюється з
нуля.

> **Обґрунтування 5.2.4 тут не діє.** Аргумент «`.work` тримає один розмір
> архіву, бо публікація — перейменування» справедливий для
> `New-BRAVOTemporaryArchivePath` у backup-конвеєрі, де partial будується
> з нуля. Trace натомість копіює попередній архів. Перенести той аргумент
> на Maintenance було б помилкою.

**3. Компресія старих каталогів** — `Compress-OldData` (рядок 6266).
Стеля = сума розмірів каталогів-кандидатів; `$oldDirs` перелічується до
циклу. Чистий ефект від'ємний, але пік додатний.

**4. Реставрація моделі** — `Restore-FromArchive` (рядок 5930) плюс
before/after-архіви (`Remove-OldRestoreArchives`, рядок 6424; патерн імен
на 6439). Найбільший споживач:

```
peak(restore) = before-архів + after-архів + розпакована_модель
```

**5. Очищення** — `Remove-BRAVOExpiredCompressedLogs` (5404),
`Remove-OldLogFiles` (6356), `Remove-OldRestoreArchives` (6424). Звільнене
місце **не віднімати**: інакше оцінка залежить від успіху видалення, і
невдале очищення робить прогін недозабезпеченим. Fail-closed, терм = 0.

#### Агрегація

Дослівно як в Archive (`BRAVO.DiskSpace.psm1:429-436`): сума пікових дельт
write-required entity на томі, і `GroupRequirementState = Known` **лише
якщо кожна** запланована операція дала межу. Один невідомий терм →
`Unknown` на всю групу → нинішній legacy-поріг.

Жодного часткового `Known`: недооцінка гірша за відсутність оцінки, бо
пропускає прогін, якому місця не вистачить.

#### Дві перешкоди, які треба вирішити явно

**Порядок виконання.** Виявлення джерел журналів іде до перевірки місця —
`ВИЯВЛЕННЯ ДЖЕРЕЛ ЖУРНАЛІВ` на рядку 7721, виклик
`Invoke-BRAVOMaintenanceDiskSpaceCheck` на 7814. Терми 1 і 3 доступні
одразу. А backlog Trace рахується значно пізніше — секція
`TRACE: ДОБОВИЙ АРХІВ І SFTP` на рядку 9561, вже після зупинки служб.
Варіанти: підняти `Get-BRAVOTraceArchiveBacklog` (3540) і
`Get-BRAVOTraceArchiveUpdatePlan` (3602) вище — вони read-only,
інвентаризація — або обмежити терм 2 грубіше, розміром каталогу Trace.
Перше точніше, друге дешевше й безпечніше.

**Реставрація.** Розмір розпакованої моделі без читання інвентаря архіву
невідомий. Але реставрація має вікно 21:00-03:00 і тижневу квоту
(`Test-BRAVORestoreTimeWindow`, рядок 548;
`Test-BRAVORestoreWeeklyQuotaConsumed`, рядок 1732), і рішення ухвалюється
до перевірки місця. Тому:

- реставрація **не запланована** цього прогону → терм 0, оцінка `Known`;
- реставрація запланована → терм `Unknown` → уся група `Unknown` →
  legacy-поріг.

Це дає безпечне поетапне впровадження: переважна більшість прогонів (усі
денні, усі поза вікном) одразу отримують справжню оцінку, а
найризикованіший сценарій лишається під старим консервативним захистом,
доки для нього не зробиться окрема межа.

#### Володіння

`Get-BRAVOMaintenanceEstimatedSpaceRequirement` у
`BRAVO.Maintenance.Runtime.ps1`, дзеркально до
`Get-BRAVOArchiveEstimatedSpaceRequirement`. **Не** в `BRAVO.DiskSpace`:
той володіє класифікацією, а не доменним знанням про операції Maintenance.

#### Валідація

За схемою 5.2.4: спершу характеризація нинішньої поведінки, далі
юніт-тести форми A24/A25/A26 — below-floor при достатній оцінці дозволяє,
недостатня оцінка блокує далі, кожен терм має власний тест, і хоча б один
іде через реальний обхід ФС, а не через інжектований override.

### Критерії приймання

1. Поведінка Archive і Maintenance на однаковому вході описана одним
   правилом, або розбіжність явно задокументована і видима в журналі.
2. Поріг, більший за ємність тому, діагностується окремим повідомленням
   із назвою фактичної ємності.
3. Захист робочого тому Maintenance не послаблено: стан, за якого місця
   реально бракує, блокує як і раніше.
4. `MaintenanceExactOnly` або стає досяжним, або видаляється з
   `ValidateSet` як мертве значення — мовчазного мертвого коду не лишається.

---

## Зміни в self-test

| Серія | Що додати |
|---|---|
| `DiskSpace` | вироджений поріг: `TotalGB` менший за поріг + достатнє вільне → Success; той самий том понад вироджений поріг → Warning з новою `Reason` |
| `DiskSpace` | контроль: `TotalGB` більший за поріг → поведінка незмінна |
| `Maintenance` | попередження preflight потрапляє в `Попереджень` |
| `Maintenance` | нумерація `[N/Total]` не зсунулась після додавання outcome |
| `ConsoleUX` | підсумок не суперечить коду завершення (`Попереджень: 0` при exit 10 неможливий) |
| `DiskSpace` | змішані причини на одному `CapacityKey` → окремий рядок на причину; однакові причини → один рядок |
| `DiskSpace` | те саме для `Problems`, не лише для `Warnings` |
| `Maintenance` | поведінка `MaintenanceWorkingVolume` при below-floor — за обраним у §4 варіантом |

Наявні `DiskSpace/S*` і `Maintenance/M*` мають лишитись зеленими **без
правок**. Якщо котрась падає — це регресія, а не очікуване оновлення.

---

## Що НЕ входить

- Зміна `MinimumFreeSpaceGB` за замовчуванням (20 GB) — site-рішення.
- Семантика `ExcludedDrives`: виключення і далі не сміє відкривати дорогу
  запису на том, де місця бракує.
- Будь-яка зміна `EstimatedRequirementNotMet` — жорстке блокування
  лишається.
- Політика `ArchivePeakSafe` і оцінка вимоги для Archive — зроблено в
  5.2.4, повторно не переглядається. §4 стосується Maintenance, не Archive.
- Формат структурованого рядка `Write-BRAVODiskSpaceDecisionLog`: він уже
  дає правдиву причину на кожну entity, і саме він лишається джерелом
  істини для §3.
