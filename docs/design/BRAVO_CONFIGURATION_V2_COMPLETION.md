# BRAVO Configuration v2 — статус завершення та обсяг робіт, що залишилися

> **ЦІЛЬОВИЙ ДОКУМЕНТ / ЩЕ НЕ ПОВНІСТЮ РЕАЛІЗОВАНО.** Цей документ описує
> обсяг робіт, що залишився для закриття FEAT-001 (`TODO_FEATURES.md`) /
> P3.1 (`ROADMAP.md`). Це довідник статусу й обсягу, а не тікет на
> реалізацію. Сам по собі він не авторизує жодних змін коду.
>
> **Звірено з `developer` 2026-09-19**, після merged #207–#212. Виконавча
> обгортка епіка — Issue #154; при розбіжності між цим документом і #154
> канонічним є #154 разом з останніми явними рішеннями власника.

## Канонічний цільовий контракт Config v2 (НОРМАТИВНО)

> Ця секція — **єдине нормативне джерело** цільової архітектури Config v2
> у цьому документі. Усі нижчі секції їй підпорядковані. Якщо нижче
> трапляється формулювання, що їй суперечить, воно застаріле й **не є
> чинною вимогою**.

**Цільова модель — двошарова** (рішення власника D2 від 2026-09-14,
зафіксоване PR #173 / `bc01fe8`):

```text
Built-in defaults   (канонічні, повні, частина пакета — НЕ файл)
    <
BRAVO.local.config  (опціональний, DATA-only, невиконуваний, site-перевизначення)
```

Нормативні пункти:

1. **`BRAVO.local.config` — єдиний підтримуваний файл site-значень.**
   Назва лишається незмінною (D2). Формат — DATA-only (плоский словник
   `'dot.path' = значення`), читається невиконуючим AST-парсером
   `ConvertFrom-BRAVOConfigurationDataFileText`
   (`modules/BRAVO.Configuration/BRAVO.Configuration.DataFile.psm1`,
   #154 B1 / PR #174). Scriptblock не створюється, не викликається і не
   dot-source'иться.
2. **`BRAVO.config.local` скасовано повністю** (D2) — ні як
   перейменування, ні як окрема сутність, ні як третій
   (машинно-локальний) шар. Жодна чинна вимога цього документа не може
   посилатися на цей файл, а операторська документація не має його
   згадувати.
3. **`BRAVO.config` — legacy, а не ціль v2.** Рішення власника від
   2026-09-14, цитоване в #154: «відмовитись від `BRAVO.config`, логіку
   перенести в дефолтну поведінку, site-файл лишити лише для локального
   перевизначення». Нормативні наслідки:
   - DATA-only формату `BRAVO.config` **не буде** — файл не
     конвертується у v2, він прибирається з пакета (B4, частина 2);
   - `BRAVO.config` не є «primary layer» цільової моделі й не є
     рекомендованим місцем site-значень;
   - на час переходу завантажувач **продовжує** читати наявний на
     сервері `BRAVO.config` наявним legacy-шляхом виконання — прибрати
     читання раніше за B5 означало б забрати в серверів їхні
     налаштування (#186);
   - комплект **ніколи** не видаляє `BRAVO.config` із сервера
     автоматично; крок виконує оператор після звірки (#154, «Out of
     scope»).
4. **Невідомий leaf лишається forward-compatible** (рішення власника D3
   від 2026-09-14, PR #173):

   ```text
   невідомий top-level блок      -> FAIL CLOSED
   невідомий батьківський вузол  -> FAIL CLOSED
   невідомий КІНЦЕВИЙ сегмент    -> ПРИЙМАЄТЬСЯ + попередження + метадані
   ```

   Це **кінцевий стан**, а не тимчасовий щабель до fail-closed.
   Обґрунтування: суворість тут коштувала б сумісності з новішим
   Configurator — файл, згенерований новішою версією, відхилявся б
   старішим комплектом цілком, і оператор лишився б без конфігурації
   взагалі. Відповідь на опечатку — **видимість**: попередження при
   завантаженні плюс
   `BravoConfigurationMetadata.LocalConfigUnknownLeafOverrides`
   (#154, A2 / PR #165).
5. **`configSchemaVersion` — маркер версії схеми site-файлу**, а не
   версія схеми комплекту. Він реалізований (#154, B3 / PR #184):
   зарезервований ключ, який знімає канонічний читач
   `Read-BRAVOLocalConfigurationOverrides` до перевірки dot-шляхів;
   файл без маркера приймається як версія `1` із попередженням і
   метаданими. `VERSION.json.configSchemaVersion` лишається `1` — це
   окреме поле з іншим предметом, і зміна метаданих релізу не
   поєднується зі зміною runtime-поведінки. Чи вимагає
   Definition-of-Done переходу site-маркера на `2` — питання B7, і цим
   документом воно не вирішене.
6. **Файл конфігурації цільової моделі — ДАНІ, а не КОД.**
   Продакшн-рантайм ніколи не виконує `BRAVO.local.config` як
   PowerShell-скрипт. Це вже виконується сьогодні. Виконуваним лишається
   лише legacy `BRAVO.config`, і саме тому він прибирається, а не
   конвертується.
7. **Секрети лишаються поза конфігурацією** — єдине місце Windows
   Credential Manager. Це вже виконується сьогодні.

### Узгодження D2 з рішенням про відмову від `BRAVO.config`

Два рішення власника від 2026-09-14 на перший погляд суперечать одне
одному, і саме ця видима суперечність була головним джерелом
розбіжностей між Issue #154, `ROADMAP.md`, `TODO_FEATURES.md` і цим
документом:

- «відмовитись від `BRAVO.config`, логіку перенести в дефолтну
  поведінку, site-файл лишити лише для локального перевизначення»
  (цитовано в #154, секція «Current state»);
- D2: «цільова модель v2 — **двошарова**:
  `Built-in defaults < BRAVO.config < BRAVO.local.config`» (PR #173).

Вони **не** взаємовиключні, і розв'язуються без вгадування:

- предмет D2 — кількість шарів перевизначення й назва локального файлу.
  D2 скасовує третій, машинно-локальний шар і перейменування. Ланцюг у
  діаграмі D2 — це пріоритет, що діє **сьогодні**, доки legacy
  `BRAVO.config` ще лежить на серверах парку;
- предмет першого рішення — доля самого `BRAVO.config`. Issue #154
  формулює його як **B4, частину 2**: «фізичне прибирання
  `BRAVO.config` з пакета», з acceptance-критерієм «`BRAVO.config`
  відсутній у пакеті, і рантайм не вимагає його»;
- merged реалізація підтверджує саме це прочитання. `BRAVO_CONFIG_LOADER.ps1`
  знає рівно два формати primary-шляху — `legacy-config` (виконання
  наявного файлу через `Read-BRAVOLegacyPrimaryRawOverrides`) і
  `synthetic-no-config`. DATA-only v2-читача для `BRAVO.config` не
  реалізовано й не заплановано жодним merged PR; натомість #186 (B4,
  частина 1) зробив цей файл **видимо застарілим**: завантаження друкує
  точні dot-шляхи, де він затінює канонічні дефолти, і радить перенести
  їх у `BRAVO.local.config`.

Отже: після завершення B5 і B4 частини 2 у пакеті лишається рівно один
файл конфігурації — `BRAVO.local.config`, — що й дає двошарову модель D2
в її кінцевому вигляді. Триелементний ланцюг у діаграмі D2 описує
перехідний стан, а не кінцеву ціль.

## Поточний стан (перевірено відносно `developer`)

P0 Configuration Foundation завершено та змерджено в `developer`
(`docs/design/BRAVO_CONFIGURATION_FOUNDATION_DESIGN.md`, CHANGELOG.md
"Не випущено (developer)" → "P0 Configuration Foundation (PR B/C)").

Завершено у Foundation:

- канонічні вбудовані значення за замовчуванням
  (`Get-BRAVODefaultConfiguration`);
- детермінований глибокий мердж (`Merge-BRAVOConfiguration`) —
  рекурсивний мердж хештаблиць, **масиви повністю замінюють масив за
  замовчуванням (а не мерджаться поелементно)**, і **явний порожній
  масив `@()` уже приймається як валідне, навмисне перевизначення** —
  перевірено безпосередньо в
  `modules/BRAVO.Configuration/BRAVO.Configuration.psm1` (коментарі
  мердж-рушія та гілка обробки масивів);
- значення за замовчуванням `maintenanceSettings.Limits.ExcludedDrives`
  **вже** дорівнює `@()` у канонічних вбудованих значеннях за
  замовчуванням — перевірено в тому ж модулі;
- розв'язання сирої конфігурації (`Resolve-BRAVORawConfiguration`);
- вилучення похідних значень (`Resolve-BRAVOConfigurationDerivation`) —
  виявлення/похідні обчислення виконуються після мерджу, а не до
  нього;
- опціональний `BRAVO.config` (конвеєр без конфігурації працює лише зі
  вбудованих значень за замовчуванням + `BRAVO.local.config`);
- явний контракт наміру `-ConfigPath` проти AUTO, послідовно
  застосований у `BRAVO_SETUP.ps1`, визначеннях завдань Task Scheduler
  та `BRAVO_CONFIGURATOR.ps1`;
- проєкція сумісності для застарілих (legacy) споживачів;
- повторна перевірка інваріантів безпеки після мерджу
  (`Test-BRAVOEffectiveSecurityInvariants` у `BRAVO_CONFIG_LOADER.ps1`):
  за замовчуванням у режимі `Enforce` ефективна конфігурація, що
  послаблює критичне для безпеки налаштування через
  `BRAVO.local.config`, відхиляється, а не приймається мовчки;
  підтримуваний режим `Warn` (`BRAVO_RUNTIME_INTEGRITY_MODE=Warn`) та
  явне перевизначення `BRAVO_ALLOW_WEAKENED_SECURITY=1` обидва
  попереджають і продовжують роботу замість відхилення, і це не
  крайові випадки, які можна проігнорувати — оператор, що використовує
  будь-який з цих режимів, не отримує безумовного відхилення.

Ефективний пріоритет **сьогодні** (перехідний стан, доки legacy
`BRAVO.config` не прибрано з серверів):

```text
DEFAULT < BRAVO.config (опціональний, legacy, виконуваний) < BRAVO.local.config (опціональний, DATA-only)
```

Цільовий пріоритет після B5 + B4 частини 2:

```text
DEFAULT < BRAVO.local.config (опціональний, DATA-only)
```

`VERSION.json` наразі декларує `"configSchemaVersion": 1`.

### Merged етапи епіка (#154)

| Етап | Стан | PR / коміт |
| --- | --- | --- |
| A1 — опис leaf наздогнав фактичну поведінку | ✅ merged | #172 (`80706f6`) |
| A2 — невідомий leaf видимий у лозі й у результаті | ✅ merged | #165 (`55ab87e`) |
| A3 — симетрія суворості primary-шару | ✅ merged | #170 (`d4716d1`) |
| A4 — стійкість parity harness (`-BaseRef` = `42cf9ad`) | ✅ merged | #171 (`3d1dc54`) |
| D2, D3 — рішення власника | ✅ merged | #173 (`bc01fe8`) |
| B0 — інструмент дельти site-конфігурації | ✅ merged | #169 (`618222b`) |
| B1 — невиконуючий AST-парсер site-файлу | ✅ merged | #174 (`b4e3a72`) |
| B2 — формальна схема (типи, fail-closed) | ✅ merged | #183 (`14b4b16`) |
| B3 — `configSchemaVersion` і диспетч за версією | ✅ merged | #184 (`cefcf50`) |
| B6 — збереження site-файлу при оновленні | ✅ merged | #185 (`29e846a`) |
| B4, частина 1 — legacy `BRAVO.config` перестає бути мовчазним | ✅ merged | #186 (`6a37efd`) |
| Parity harness у CI | ✅ merged | #193 (`b9bd4dc`) |
| Pilot tooling — артефакт, оркестратор і фікси реальних прогонів | ✅ merged | #207–#212 |
| **B5** — міграція парку | ⬜ не зроблено | — |
| **B4, частина 2** — прибирання `BRAVO.config` з пакета | ⬜ не зроблено | блокується B5 |
| **B7** — DoD regression matrix + parity як required check | ⬜ не зроблено | — |

### Стан pilot tooling (#207–#212)

Контрольований Config v2 Pilot — це **реалізований**, merged
інструментарій, а не план. Він живе в `deploy/` і покритий self-test
suite-ом `selftest/BRAVO_SELF_TEST.ConfigV2PilotArtifact.ps1`:

- `deploy/Get-BRAVOConfigSiteDelta.ps1` — друкує готові dot-шляхи
  site-дельти для конкретного сервера (B0, #169);
- `deploy/New-BRAVOConfigV2PilotArtifact.ps1` та
  `deploy/Test-BRAVOConfigV2PilotArtifact.ps1` — побудова й перевірка
  pilot-артефакта (#208);
- `deploy/Start-BRAVOConfigV2Pilot.ps1` разом із
  `deploy/BRAVOConfigV2Pilot.Runtime.ps1` — канонічний оркестратор
  життєвого циклу (`Preflight → Prepare → Approve → Activate → Validate
  → Accept`), hash-bound approval, TOCTOU-gate і взаємне виключення
  мутуючих операцій над одним `InstallRoot` (#208);
- `deploy/Compare-BRAVOConfigEffectiveSnapshot.ps1` — порівняння
  ефективних знімків BEFORE/AFTER (#205);
- runbook-и: `docs/BRAVO_CONFIG_V2_PILOT_RUNBOOK.md`,
  `docs/BRAVO_CONFIG_V2_PILOT_MIGRATION_RUNBOOK_20260916.md`.

Дефекти, знайдені **реальними** прогонами на throwaway VM, і те, що
кожен фікс довів:

| PR | Дефект, знайдений прогоном | Регресія |
| --- | --- | --- |
| #207 | `-ValidateOnly` персистував стан через legacy-baseline міграцію; додано явний offline-access контракт (`-AllowOfflineExternalAccess`) замість мовчазного пропуску мережевих проб; побічно: array-splat не прив'язує switch-параметри в PowerShell 5.1 | F20 |
| #208 | Контрольований pilot-артефакт: hash-bound approval, взаємне виключення `-Activate`/`-Rollback` над одним `InstallRoot`, точний опис механізму `git show` | F1–F17 |
| #209 | `Test-BRAVOPilotCandidateSyntax` відхиляла `$true`/`$false`/`$null` у candidate — AST подає їх як `VariableExpressionAst`, тож легітимний boolean-override блокував `-Activate` | F5b, F5c |
| #210 | Оркестратор падав власним `ParameterBindingValidationException` на порожньому виводі дочірнього `BRAVO_SETUP.ps1`, ховаючи первинну помилку (`[string[]]` без `[AllowEmptyCollection()]`) | F18, F19 |
| #211 | Preflight credential-check шукав «No matching credentials», а реальний `cmdkey.exe` на Windows Server 2022 для відсутнього target друкує `* NONE *` — перевірка завжди звітувала `Found=$true` | F23 |
| #212 | `if/else`-як-вираз під `Set-StrictMode 2.0` колапсував 0-елементний масив у `$null` (`$discoveryDrift.Count` кидав `PropertyNotFoundException`); `Get-BRAVOPilotHealthDegradationLines` відхиляв порожній (не `$null`) масив рядків health-before | `DiscoveryBaseline/DriftAssignmentSurvivesStrictModeCountAccess`, F24 |

**Що це означає для B5.** Інструментарій міграції вже існує, і дефекти,
виявлені двома реальними e2e-прогонами, виправлені. Бракує не коду, а
**виконаних міграцій і доказів**: чистого pilot-прогону на
disposable-хості після #212, а потім послідовної міграції парку.

### `BRAVO.local.config` сьогодні — non-executing AST-parser (#154, B1)

> **Оновлено 2026-09-14 (задача B1).** Розділ нижче описує ПОТОЧНУ
> реалізацію. Попередня редакція описувала механізм
> «restricted-language, потім виконати» — його більше немає для
> site-шару; історія рішення лишається наприкінці розділу, бо саме
> вона пояснює, чому заміна була потрібна.

`BRAVO.local.config` — це опціональний файл перевизначень у форматі
даних (хештаблиця шлях-через-крапку → значення). Його продакшн-
завантажувач, перевірений у `BRAVO_CONFIG_LOADER.ps1`
(`Read-BRAVOLocalConfigurationOverrides`), більше не компілює й не
викликає файл: він передає текст канонічному парсеру

```powershell
ConvertFrom-BRAVOConfigurationDataFileText -Text $localOverrideText -SourceName $localOverridePath
```

(`modules/BRAVO.Configuration/BRAVO.Configuration.DataFile.psm1`),
який парсить текст у AST
(`[System.Management.Automation.Language.Parser]::ParseInput`) і
обходить його за **явним переліком дозволених вузлів-літералів**.

Дозволено рівно це:

- верхній рівень — рівно один hashtable-літерал `@{ ... }`;
- ключі — рядкові константи, непорожні, без повторів
  (регістронезалежно, як і в самій hashtable PowerShell);
- значення — рядкові/числові константи, `$true`, `$false`, `$null`,
  масиви `@( ... )` та `1, 2, 3`, вкладені hashtable-літерали, знак
  `+`/`-` безпосередньо перед числовою константою (форма запису
  числа, а не обчислення).

Усе інше — відмова (fail closed): `param()`/`begin`/`process`/
`dynamicparam`/`using`, виклики команд і функцій, доступ до членів,
індексація, приведення типів, підвирази, будь-які інші звернення до
змінних (включно з `$env:`, `$global:`, `$script:`, довільне `$foo` —
і `$global:true`, що лише СХОЖИЙ на дозволену константу), рядки з
підстановкою, дужкові вирази, оператори.

Контракт пунктів 1–11 із «Вимоги до безпечного декларативного
парсера» нижче виконано для site-шару повністю: scriptblock не
створюється, не викликається і не dot-source'иться.

**Чому попереднього механізму було недостатньо (історія рішення).**
Раніше тут було:

```powershell
$localOverrideScript = [scriptblock]::Create($localOverrideText)
$localOverrideScript.CheckRestrictedLanguage([string[]]@(), [string[]]@(), $false)
$localOverrideData = & $localOverrideScript
```

`CheckRestrictedLanguage` із порожніми списками дозволених команд і
змінних справді відхиляв виклики cmdlet, виклики функцій і звернення
до змінних/середовища ще до виклику блока — це був реальний і
ефективний контроль. Але:

- **це не було гарантією «лише літерали»**: обмежена мова даних
  PowerShell навіть із порожніми allow-списками все ще приймає деякі
  форми виразів (наприклад, арифметичні та діапазонні);
- **перевірений `ScriptBlock` потім ВИКЛИКАВСЯ** (`& $localOverrideScript`),
  тому будь-який такий дозволений вираз *обчислювався*, а не
  вилучався як літерал — `1 + 1` у значенні ставало `2`;
- **перелік дозволеного належав граматиці PowerShell, а не нам**:
  розширення обмеженої мови в майбутній версії платформи автоматично
  розширило б і те, що приймає site-файл.

Сам `BRAVO.config` (primary-шар) **усе ще** виконуваний
PowerShell-скрипт. Він не переводиться на DATA-only формат — він
прибирається з пакета (B4, частина 2); див. канонічний контракт на
початку документа.

> **Наслідок для решти документа.** Далі в тексті трапляється опис
> «legacy-пари» як «виконуваний `BRAVO.config` + restricted-language
> `BRAVO.local.config`». Після B1 справедлива лише ПЕРША половина цього
> опису: site-файл більше не є restricted-language-then-invoke. Там, де
> документ говорить про поведінку site-шару СЬОГОДНІ, авторитетним є цей
> розділ разом із канонічним контрактом на початку документа.
## Прогалини, що залишилися (що ще потрібно для Config v2)

Звірено з `developer` (`66ce47f`, 2026-09-19).

Залишилося рівно три робочі етапи — саме ті, що перелічені в #154:

- **B5 — міграція парку.** На кожному сервері site-значення мають жити в
  `BRAVO.local.config`, а не в `BRAVO.config`; `BRAVO_SETUP.ps1
  -ValidateOnly` до і після міграції має давати ті самі ефективні
  значення. Інструментарій merged (див. «Стан pilot tooling» вище) —
  бракує виконаних міграцій і доказів.
- **B4, частина 2 — фізичне прибирання `BRAVO.config` з пакета.**
  Блокується B5. Окремими комітами: фікстури self-test → parity harness →
  видалення файлу. Кореневий `BRAVO.config` сьогодні є джерелом фікстур
  для кількох self-test-suite-ів і для самого parity harness, тому
  видалення файлу без попереднього розчеплення фікстур зламало б єдиний
  обов'язковий доказ цієї ж зміни. Точну кількість читань треба
  перерахувати перед початком роботи, а не брати з історичних записів.
- **B7 — Definition-of-Done regression matrix на фінальному шляху
  завантаження** плюс промоція parity harness у required status check.

Що **не** є залишковим gap-ом і чого реалізовувати не треба:

- ~~DATA-only формат `BRAVO.config`~~ — знято: файл прибирається, а не
  конвертується (канонічний контракт, пункт 3);
- ~~канонічна назва/контракт файлу `BRAVO.config.local`~~ — знято
  рішенням D2;
- ~~окремий машинно-локальний шар~~ — знято рішенням D2;
- ~~невиконуючий декларативний парсер site-шару~~ — зроблено
  (#154 B1 / PR #174);
- ~~формальна схема типів~~ — зроблено (#154 B2 / PR #183);
- ~~маркер `configSchemaVersion` і диспетч за версією~~ — зроблено
  (#154 B3 / PR #184);
- ~~збереження site-файлу при оновленні~~ — зроблено (#154 B6 / PR #185);
- ~~інструмент дельти site-конфігурації~~ — зроблено (#154 B0 / PR #169);
- ~~видимість застарілого `BRAVO.config`~~ — зроблено
  (#154 B4 ч.1 / PR #186);
- ~~автоматичний прогін parity harness~~ — зроблено (PR #193);
- ~~інструмент міграції (dry-run, стейджування, активація, відкат)~~ —
  зроблено як контрольований pilot (#208–#212). Окремого
  `BRAVO_CONFIG_MIGRATE.ps1` не існує і не потрібно: канонічна точка
  входу — `deploy/Start-BRAVOConfigV2Pilot.ps1`.

Предметом B7 лишається:

- повна регресійна матриця Definition-of-Done, прогнана саме проти
  ФІНАЛЬНОГО шляху завантаження, а не проти helper-ів в обхід нього;
- рішення щодо `configSchemaVersion = 2` для site-маркера (див. «Схема
  v2» нижче);
- промоція `Config parity (BRAVO_CONFIG_LOADER)` у required status check
  — адміністративна дія в налаштуваннях GitHub.

## Цільова архітектура

```text
Built-in defaults      (канонічні, повні, частина пакета — не файл)
    <
BRAVO.local.config     (опціональний, DATA-only, site-перевизначення)
```

Вбудовані значення за замовчуванням — це канонічні, повні значення за
замовчуванням продукту/пакета; вони не «зберігаються в `BRAVO.config`».
`BRAVO.local.config` — це **опціональний шар перевизначення рівня
сайту** поверх цих значень. Конвеєр без жодного файлу конфігурації
працює лише зі вбудованих значень за замовчуванням і має продовжувати
працювати так завжди.

Не описуйте `BRAVO.local.config` як «значення за замовчуванням пакета»
чи «первинну конфігурацію, версійовану разом із пакетом» — це
неправильно представляло б, де насправді зберігаються значення за
замовчуванням.

Файл конфігурації — це ДАНІ (DATA), а не КОД (CODE).
Продакшн-рантайм не виконує його як PowerShell-скрипт.

**Перехідний стан.** Доки B5 і B4 частина 2 не завершені, наявний на
сервері legacy `BRAVO.config` лишається читабельним середнім елементом
ланцюга (`DEFAULT < BRAVO.config < BRAVO.local.config`) і виконується
наявним legacy-шляхом. Це підтримуваний перехідний режим, а не ціль.

### Вимога до безпечного декларативного парсера (site-шар — виконано, B1)

Парсер v2 повинен:

1. парсити текст PowerShell в AST / еквівалентне декларативне
   представлення;
2. валідувати лише дозволені структури літеральних даних;
3. вилучати значення як ДАНІ;
4. **ніколи не викликати результуючий `ScriptBlock` конфігурації**;
5. ніколи не робити dot-source файлу конфігурації;
6. ніколи не використовувати `& $scriptBlock` (або еквівалентний
   виклик) для продакшн-файлів Configuration v2;
7. відхиляти будь-який непідтримуваний синтаксис за принципом fail
   closed;
8. не дозволяти жодних cmdlet;
9. не дозволяти жодних викликів функцій;
10. не дозволяти звернень до змінних/середовища, **за винятком** трьох
    внутрішніх літеральних констант PowerShell `$true`, `$false` та
    `$null` — вони необхідні для представлення булевих та допускаючих
    null значень конфігурації, і мають розпізнаватися явно за
    ідентичністю AST/токена як літеральні ЗНАЧЕННЯ ДАНИХ, ніколи не
    розв'язувані зі стану сесії/середовища. Усі інші форми змінних
    залишаються відхиленими: `$env:...`, `$global:...`,
    `$script:...`, `$local:...`, довільне `$foo`, `${...}` та будь-яке
    інше звернення до змінної;
11. не дозволяти довільних виразів.

**Стан на 2026-09-14 (задача B1).** Для site-шару
(`BRAVO.local.config`) пункти 1–11 виконано:
`ConvertFrom-BRAVOConfigurationDataFileText`
(`modules/BRAVO.Configuration/BRAVO.Configuration.DataFile.psm1`)
парсить текст у AST, вилучає значення за власним fail-closed переліком
дозволених вузлів-літералів і не створює й не викликає scriptblock.
Пункт 10 реалізовано саме так, як він тут сформульований: `$true`,
`$false` і `$null` розпізнаються за точним `VariablePath.UserPath`,
який зберігає префікс області, тому `$global:true` відхиляється як
звичайне звернення до змінної.

Попередній механізм (`CheckRestrictedLanguage` + `& $scriptBlock`)
доводив лише пункти 8–10, **порушував пункт 4** (перевірений блок
викликався) і **не доводив** пунктів 7 та 11: `CheckRestrictedLanguage`
застосовує власну обмежену граматику мови PowerShell, а не специфічний
fail-closed перелік дозволених literal-AST, і ця граматика все ще
приймає деякі форми виразів (наприклад, арифметичні/діапазонні), які
під час виклику обчислювались.
Для legacy `BRAVO.config` цей контракт **не буде** виконано і не
повинен: файл не переводиться на DATA-only формат, він прибирається з
пакета (B4, частина 2 — див. канонічний контракт, пункт 3). До моменту
прибирання він лишається виконуваним, і саме тому `SECURITY.md`
розглядає його як довірений операторський файл.

Справжній non-executing прецедент лише на основі AST уже існує в цій
кодовій базі для вужчої мети: `Test-BRAVORuntimeSecuritySettings`
(`BRAVO_RUNTIME_GUARD.ps1`) читає конкретні літерали перемикачів
безпеки з `BRAVO.config` через
`[Management.Automation.Language.Parser]::ParseFile` — парсинг AST —
**ніколи не викликаючи `BRAVO.config`**, саме тому, що завантаження
`BRAVO.config` як коду означало б виконання довільного PowerShell ще до
запуску перевірки безпеки (див. `SECURITY.md`, «Перемикачі безпеки»).
Ця функція вилучає лише невеликий фіксований набір літеральних значень
для однієї мети; це не універсальний читач конфігурації.

### Доля legacy `BRAVO.config`

> Ця секція замінює скасовані секції «Ціль `BRAVO.config` (v2)», «Ціль
> `BRAVO.config.local` (v2)» і «Поточне іменування проти цільового», які
> описували цільовий DATA-only primary-шар і третій машинно-локальний
> шар. Обидві моделі скасовані — див. канонічний контракт на початку
> документа.

- `BRAVO.config` **не** отримує формату v2 і **не** є цільовим шаром;
- він свідомо виключений із копіювання при оновленні
  (`deploy/Update-BRAVOServer.ps1`) — це файл сервера, не комплекту, і
  саме тому сервер із конфігом старої епохи роками не отримує покращень
  дефолтів;
- з #186 (B4, частина 1) завантаження більше не мовчить про це: воно
  рахує реальну різницю між значеннями файлу й канонічними дефолтами й
  повідомляє точні dot-шляхи — у попередженні та в
  `BravoConfigurationMetadata.PrimaryConfigOverridesCanonicalDefaults`;
- шлях перенесення (`deploy/README.md`):
  1. `deploy\Get-BRAVOConfigSiteDelta.ps1` — друкує готові dot-шляхи
     саме для цього сервера;
  2. перенести їх у `BRAVO.local.config` (переживає оновлення);
  3. `BRAVO_SETUP.ps1 -ValidateOnly` — переконатися, що ефективні
     значення не змінились;
  4. лише після цього прибрати `BRAVO.config` — крок виконує оператор
     свідомо;
- B4, частина 2 прибирає файл **з пакета**; комплект ніколи не видаляє
  його із сервера сам;
- операторська документація (`README.md`, `BRAVO_SETUP.md`,
  `OPERATIONS.md`) інструктує операторів використовувати
  `BRAVO.local.config` і ніколи — `BRAVO.config.local`.

### Перехід застарілого (legacy) `BRAVO.config`

- legacy `BRAVO.config` не видаляється із сервера в момент випуску
  цільової моделі й не видаляється комплектом ніколи; він залишається
  читабельним і підтримуваним на період переходу;
- процедура міграції (dry-run, поетапна конвертація) переносить
  site-дельту з legacy `BRAVO.config` у `BRAVO.local.config` — це **той
  самий** файл із тією самою назвою, що вже є в продакшні, а не
  проміжний файл із новою назвою. Канонічна точка входу —
  `deploy/Start-BRAVOConfigV2Pilot.ps1`; проміжний кандидат живе в
  неактивній зоні стейджування, а не під активною назвою;
- резервне копіювання та відкат стосуються транзакції активації, а не
  створення проміжного кандидата як такого;
- legacy та цільовий стан повинні давати еквівалентну ефективну
  конфігурацію на контрольних фікстурах протягом перехідного вікна:
  `EffectiveConfig(BEFORE) == EffectiveConfig(AFTER)`, з урахуванням
  ТИПІВ значень (`$true` ≠ `1`, `30` ≠ `'30'`, масив ≠ скаляр).

### Межа безпеки legacy/цільового стану під час переходу

Поки завантажувач приймає обидва стани протягом вікна міграції, ці два
шляхи мають залишатися суворо розділеними:

- `BRAVO.local.config` завжди лише парситься й вилучається як ДАНІ — він
  ніколи не виконується як код, навіть якщо не проходить валідацію;
- legacy `BRAVO.config` використовує лише наявний, уже перевірений шлях
  сумісності (сьогоднішнє виконання файлу через
  `Read-BRAVOLegacyPrimaryRawOverrides`);
- недійсний або пошкоджений `BRAVO.local.config` **fail closed** — він
  відхиляється й ніколи не повинен мовчки повторно оброблятися через
  виконуваний шлях legacy. Файл не стає виконуваним лише тому, що його
  парсинг зазнав невдачі.

### Активний режим набору конфігурації (контракт визначення формату)

> **Переписано під двошарову модель D2.** Попередня редакція цієї секції
> містила матриці на три файли з колонкою `BRAVO.config.local` і рядками
> про конфлікт двох імен локального шару. Та модель скасована рішенням
> D2; матриці нижче відповідають фактичному та цільовому контракту.

Завантажувач може класифікувати окремі файли, але перш ніж мерджити
будь-який шар, він повинен визначити **один узгоджений активний режим
конфігурації** для інсталяції.

**Визначення формату site-шару завжди non-executing.** Класифікація
`BRAVO.local.config`, зокрема зняття маркера `configSchemaVersion`,
ніколи не виконує файл — вона працює на AST, узгоджено з правилом
fail-closed вище.

**Матриця активного режиму та наміру (AUTO проти EXPLICIT).** Матриця
містить окремий вимір **наміру** — AUTO (оператор явно не вказав
`-ConfigPath`) проти EXPLICIT (оператор явно вказав `-ConfigPath
<шлях>`). Канонічний інваріант: **EXPLICIT `-ConfigPath` означає, що
явно вибраний основний файл ПОВИНЕН існувати.** Тому EXPLICIT +
відсутній основний файл = FAIL CLOSED незалежно від наявності
site-файлу. Режими з відсутнім основним файлом валідні ТІЛЬКИ для наміру
AUTO. Цей контракт уже перевірений у
`selftest/BRAVO_SELF_TEST.ConfigLoader.ps1` та `ConfigIntent.ps1` — цей
документ йому не суперечить.

**Підтримувані активні режими:**

| # | `BRAVO.config` (legacy) | `BRAVO.local.config` | Намір | Результат | Пріоритет |
|---|---|---|---|---|---|
| A | відсутній | відсутній | AUTO | SUPPORTED — лише built-in | `DEFAULT` |
| B | присутній | відсутній | AUTO/EXPLICIT | SUPPORTED — TRANSITION MODE | `DEFAULT < BRAVO.config` |
| C | присутній | присутній | AUTO/EXPLICIT | SUPPORTED — TRANSITION MODE | `DEFAULT < BRAVO.config < BRAVO.local.config` |
| D | відсутній | присутній | AUTO | SUPPORTED — TARGET MODE | `DEFAULT < BRAVO.local.config` |

Режим D — **цільовий**. Режими B і C підтримуються лише доти, доки
конкретний сервер не пройшов B5; після B4 частини 2 комплект більше не
постачає `BRAVO.config`, але завантажувач продовжує читати той файл,
який оператор лишив на місці.

**Заборонені комбінації для наміру EXPLICIT з відсутнім основним
файлом — fail closed незалежно від стану site-файлу:**

| # | `BRAVO.config` (legacy) | `BRAVO.local.config` | Намір | Результат |
|---|---|---|---|---|
| E | відсутній | відсутній | EXPLICIT | FAIL CLOSED — явно вибраний основний шлях не існує |
| F | відсутній | присутній | EXPLICIT | FAIL CLOSED — явно вибраний основний шлях не існує, наявність site-файлу не рятує |

**Явно заборонені стани — fail closed, без неявного пріоритету:**

| Стан | Результат |
|---|---|
| `BRAVO.local.config` декларує непідтримуваний/невідомий `configSchemaVersion` | FAIL CLOSED. |
| `BRAVO.local.config` містить невідомий top-level блок | FAIL CLOSED. |
| `BRAVO.local.config` містить невідомий БАТЬКІВСЬКИЙ вузол | FAIL CLOSED. |
| `BRAVO.local.config` містить невідомий КІНЦЕВИЙ сегмент (leaf) | ПРИЙМАЄТЬСЯ + попередження + метадані (рішення D3) — це НЕ fail-closed випадок. |
| `BRAVO.local.config` провалює валідацію типів | FAIL CLOSED. |
| `BRAVO.local.config` містить непідтримуваний синтаксис (виклик, довільний вираз, звернення до змінної, окрім літералів `$true`/`$false`/`$null`) | FAIL CLOSED — і файл при цьому ніколи не виконується. |
| ефективна конфігурація послаблює критичний для безпеки параметр у режимі `Enforce` | FAIL CLOSED (`Test-BRAVOEffectiveSecurityInvariants`). Режим `Warn` і явний `BRAVO_ALLOW_WEAKENED_SECURITY=1` — задокументовані винятки, що попереджають і продовжують. |
| формат `BRAVO.local.config` невідомий/неоднозначний | FAIL CLOSED. |

**Недійсний найвищий пріоритетний шар.** `BRAVO.local.config` —
найвищий за пріоритетом шар. Це вимагає, щоб він мав і найсуворішу
семантику відмови: будь-який malformed, непідтримуваний чи такий, що
провалює схему, site-файл **fail closed** — незалежно від того, чи
legacy `BRAVO.config` присутній або відсутній. Обов'язкові інваріанти:

- недійсний `BRAVO.local.config` НІКОЛИ не призводить до мовчазного
  повернення до конфігурації лише з legacy/DEFAULT («NO FALLBACK»);
- недійсний `BRAVO.local.config` НІКОЛИ не призводить до часткового
  мерджу підмножини валідних ключів разом з відхиленими
  («NO PARTIAL MERGE»);
- недійсний `BRAVO.local.config` НІКОЛИ не ігнорується мовчки на користь
  продовження без нього («NO IGNORE-AND-CONTINUE»);
- завантажувач валідує/класифікує site-шар ДО прийняття фінальної
  ефективної конфігурації — синтаксична чи схемна невдача цього шару
  зупиняє побудову ефективного набору повністю;
- це узгоджується з, а не послаблює, збережену семантику fail closed для
  EXPLICIT з відсутнім основним файлом (рядки E–F вище).

### Схема v2

> **Оновлено 2026-09-15 (задача B2).** Формальна схема реалізована:
> `modules/BRAVO.Configuration/BRAVO.Configuration.Schema.psm1`
> (`Get-BRAVOConfigurationSchema`, `Test-BRAVOConfigurationOverrideSchema`).
> Нижче — актуальний стан. Маркер версії реалізовано окремо (B3, нижче);
> решта пунктів цієї секції лишається регресійною вимогою B7.
>
> **Джерело форми — канонічні дефолти, не друга таблиця.** Схема
> виводиться з `Get-BRAVODefaultConfiguration`: для кожного dot-шляху
> рід `Node | Boolean | String | Number | Array`, рід елементів масиву й
> допустимість `$null`. Руками задано лише те, чого з дефолту вивести
> НЕМОЖЛИВО — 9 листів `discoverySettings.*` (дефолт `$null`) і
> `maintenanceSettings.Limits.ExcludedDrives` (дефолт `@()`). Лист із
> дефолтом `$null` без явного запису валить побудову схеми, тому
> «обов'язковість» покриття доводиться механічно, а не домовленістю.
>
> **Місце в конвеєрі.** `Complete-BRAVOConfigurationLoad` валідує типи
> site-шару ПІСЛЯ невиконуючого вилучення даних (B1) і ДО мерджу, тож
> недійсне значення не встигає стати ефективним. Повідомлення називає
> точний dot-шлях, включно з індексом елемента масиву.
>
> **Обсяг B2 свідомо звужений.** Схема НЕ перевіряє діапазони, переліки
> допустимих значень і семантику шляхів: у них уже є канонічні власники
> (`Assert-BravoLoadedConfiguration`, `Test-BRAVOEffectiveSecurityInvariants`,
> каталог `BRAVO.Configurator.Schema.psd1`), і сьогодні вони поводяться як
> «попередження + безпечний дефолт» або мають задокументований аварійний
> перемикач. Копія тих правил у схемі була б дубльованою політикою, а
> переведення їх у fail-closed — зміною поведінки, яка потребує окремого
> рішення. Замість копії діє механічний guard
> `Schema/DocumentedCatalogKindsAgree`.
>
> **Legacy `BRAVO.config` типами не перевіряється** — він прибирається
> задачею B4; асиметрія суворості шарів (F1) лишається предметом A3,
> яка робить її видимою через попередження.
>
> **Оновлено 2026-09-15 (задача B3).** Маркер версії реалізовано:
> `Get-BRAVOConfigurationSchemaVersionContract`,
> `Resolve-BRAVOConfigurationSchemaVersion` і
> `Get-BRAVOConfigurationSchemaVersionDeclarationLine` у тому ж модулі.
>
> **Маркер — зарезервований ключ, а не перевизначення.** Site-файл є
> плоским словником `'dot.path' = значення`, тому `configSchemaVersion`
> синтаксично є односегментним, тобто TOP-LEVEL шляхом, і
> `ConvertTo-BRAVONestedOverride` відхилив би його fail-closed. Маркер
> знімає єдиний канонічний читач `Read-BRAVOLocalConfigurationOverrides`
> ДО перевірки шляхів.
>
> **Файл без маркера.** Цього випадку не визначала жодна секція
> попередньої редакції документа. Рішення для B3: файл без маркера
> приймається як версія 1 із попередженням і метаданими. Відмова
> зупинила б кожен сервер із наявним site-файлом на першому ж оновленні,
> тобто була б аварією парку, а не міграцією. Вікно закривається тим, що
> обидва серіалізатори site-файлу пишуть маркер автоматично.
>
> **`VERSION.json.configSchemaVersion` не змінено.** Там версія схеми
> самого комплекту; поєднувати зміну метаданих релізу зі зміною
> runtime-поведінки заборонено політикою релізу. Метадані завантаження
> розводять ці поняття полями `LocalConfigDeclaredSchemaVersion` і
> `LocalConfigEffectiveSchemaVersion`.
>
> **Чи потрібен перехід site-маркера на `2`** — питання Definition-of-Done
> (B7), і воно свідомо лишається відкритим тут: сьогодні site-файл
> ефективно має версію `1`, і жоден merged PR не оголошував `2`.


`configSchemaVersion` — це назва поля/семантичний ідентифікатор
маркера версії схеми. Оскільки завантажувач v2 парсить файл як
PowerShell AST/DATA, єдина канонічна серіалізована форма цього
маркера — `configSchemaVersion = 2` (PowerShell hashtable-присвоєння);
форма з двокрапкою (`configSchemaVersion: 2`) НЕ є валідним
PowerShell DATA-синтаксисом і ніде в цьому документі не позначає
фактичний вміст файлу v2 — серіалізатор повинен завжди emit-ити лише
форму зі знаком рівності, а класифікатор/парсер не повинен покладатися
на невалідний альтернативний запис із двокрапкою.

- явний маркер `configSchemaVersion = 2`, що перевіряється під час
  завантаження;
- невідомі ключі провалюють валідацію (fail-closed, а не
  warn-and-ignore) — **окрім невідомого кінцевого сегмента (leaf)**:
  рішенням D3 (2026-09-14) для нього збережено forward-compat, він
  приймається й повідомляється (попередження плюс метадані), а не
  відхиляється;
- недійсні типи провалюють валідацію;
- масиви повністю замінюють відповідний масив за замовчуванням — **вже
  поведінка мердж-рушія Foundation сьогодні**; схема v2 повинна
  перенести це через новий декларативний шлях завантаження й повторно
  довести, а не реалізувати заново;
- явний порожній масив (`@()`) є валідним, навмисним перевизначенням
  — **уже приймається сьогодні** у мердж-рушії Foundation; схема v2
  повинна зберегти це й додати регресійне покриття для шляху
  завантаження v2;
- значення за замовчуванням `Limits.ExcludedDrives` дорівнює `@()` —
  **уже вбудоване значення за замовчуванням сьогодні**; схема v2 не
  повинна це регресувати.

### Канонічний конвеєр валідації

Канонічний конвеєр валідації (застосовується як під час поетапної
конвертації кандидата міграції, так і продакшн-завантажувачем):

```text
текст → PowerShell parse → строга валідація дозволеного синтаксису → non-executing вилучення ДАНИХ →
валідація версії схеми → валідація невідомих ключів (з винятком D3 для leaf) → валідація типів →
мердж сирого шару → похідні обчислення → перевірка ефективних інваріантів безпеки
```

Поетапне стейджування кандидата повинно проходити всі етапи
парсингу/схеми, застосовні до самостійного кандидата, перш ніж називати
його validated; продакшн-завантажувач повинен викликати ТУ САМУ
бібліотеку парсера/схеми — не другу, окрему реалізацію.

### Міграція: конвертація проти активації

> **Реалізовано як контрольований pilot (#208–#212).** Канонічна точка
> входу — `deploy/Start-BRAVOConfigV2Pilot.ps1` зі станами `Preflight →
> Prepare → Approve → Activate → Validate → Accept`. Окремого
> `BRAVO_CONFIG_MIGRATE.ps1` не існує і не потрібно. Нижче — контракт,
> якому цей інструментарій має відповідати.
Міграція має дві окремі стадії, які не можна змішувати:

```text
CONVERSION:
legacy config -> validated staged v2 candidate

ACTIVATION:
staged v2 candidate -> active production v2 configuration
```

**Конвертація / стейджування:**

- канонічна точка входу міграції (`deploy/Start-BRAVOConfigV2Pilot.ps1`)
  конвертує лише поля, чия провенанс (provenance) доведена як
  специфічна для сайту згідно з контрактом провенансу нижче — а не
  просто «явно ідентифіковані» без визначення способу ідентифікації;
- виявлені/згенеровані значення ніколи не стають постійними
  перевизначеннями через міграцію;
- вихідний(і) legacy-файл(и) читаються, але ніколи не змінюються
  конвертацією;
- сконвертований кандидат записується в неактивне місце стейджування
  — шлях, який поточний продакшн-завантажувач не споживає — ніколи під
  активною назвою `BRAVO.local.config` і ніколи поверх legacy
  `BRAVO.config`;
- проміжний кандидат валідується тим самим non-executing парсером, тим
  самим валідатором схеми (див. «Канонічний конвеєр валідації v2»
  вище) і порівнюється з ефективною конфігурацією legacy на предмет
  еквівалентності, перш ніж вважатися валідним кандидатом;
- проміжний кандидат може бути відкинутий і перегенерований у
  будь-який момент без впливу на активну інсталяцію; вихідний файл,
  що передував міграції, залишається недоторканим, а не просто
  «збереженим як резервна копія»;
- сама конвертація ніколи не змінює те, що читає продакшн-завантажувач.

**Контракт провенансу перед фільтрацією legacy-полів.** Виконуваний
legacy `BRAVO.config` присвоює повну модель конфігурації через блоки
`$global:*` незалежно від того, чи оператор колись редагував
конкретне значення. Сам факт наявності присвоєння в файлі — це
**ідентичність знімку джерела (source snapshot identity)**, а НЕ
**походження поля (field provenance)**. Наявний хеш/базова лінія
джерела, що використовується захистом дрейфу шляху активації, доводить
лише, що джерело не змінилося ПІСЛЯ стейджування кандидата — він НЕ
доводить, які значення є оператор-вибраними перевизначеннями.
Стейджування не може одночасно (а) серіалізувати лише специфічні для сайту поля і (б)
довести ефективну еквівалентність, якщо просто діффити активний
legacy-файл проти довільного «поточного» набору значень за
замовчуванням: закріплення застарілих поставлених значень за
замовчуванням стає постійним, а немарковані реальні перевизначення
можуть бути втрачені.

*Джерела провенансу.* Міграція може класифікувати поле лише на основі
довірених доказів, таких як: (1) точна ідентичність лінії/релізу
legacy-конфігурації; (2) точна pristine поставлена базова лінія, що
відповідає доведеній лінії; (3) довірені метадані провенансу
інсталятора/updater'а, якщо такі метадані існують у майбутній
реалізації (**PLANNED/PROPOSED** — сьогодні не існує й не
стверджується як існуючий механізм); (4) явний вибір/підтвердження
міграції з боку оператора. Точні шляхи зберігання та назви полів —
деталь реалізації.

*Заборона порівняння з непов'язаним поточним значенням за
замовчуванням.* Визначення оператор-перевизначень шляхом простого
порівняння старого персистентного legacy-конфіга з поточним
`BRAVO.config` репозиторію, поточними значеннями за замовчуванням
пакета, цільовими значеннями за замовчуванням v2 чи значеннями за
замовчуванням останнього релізу — ЗАБОРОНЕНО, якщо ця база не доведена
як pristine базова лінія саме для тієї лінії legacy-конфігурації, що
мігрується. Інсталяція могла зберегти свій legacy `BRAVO.config` через
декілька оновлень toolkit-у. Тому **поточна версія пакета НЕ
дорівнює автоматично версії провенансу конфігурації**. Якщо лінію
неможливо довести: провенанс = AMBIGUOUS.

*Семантичне порівняння.* Там, де точний pristine провенанс доступний,
порівнюються нормалізовані семантичні legacy-поля, а не текстове
форматування/коментарі. Концептуальний конвеєр: активне legacy-джерело
→ виділена legacy-екстракція → нормалізована сира конфігурована модель
↔ відповідна pristine legacy-базова лінія → та сама сумісна
нормалізація → pristine сира конфігурована модель → порівняння
семантики полів → класифікація провенансу. Це виконується ДО
виявлення/деривації. Згенеровані/похідні значення лишаються
виключеними з постійних v2-перевизначень.

*Класифікація полів.* Кожне мігроване поле повинно завершитися одним
явним класом: PROVEN OPERATOR / LOCAL DEVIATION; PROVEN SHIPPED
DEFAULT; DERIVED / GENERATED; EXPLICITLY CONFIRMED BY OPERATOR;
AMBIGUOUS (точні назви enum — деталь реалізації). Правила: активне
семантичне значення відрізняється від точної доведеної pristine
базової лінії ⇒ кандидат на локальне відхилення (local deviation);
активне значення дорівнює точній доведеній pristine базовій лінії ⇒
кандидат на shipped-default — якщо тільки довірений явний провенанс
оператора не каже, що значення навмисно закріплене як перевизначення.
Згенеровані/похідні поля НІКОЛИ не серіалізуються лише тому, що вони
з'являються в ефективному legacy-стані. Неоднозначні поля НІКОЛИ не
вгадуються мовчки.

*Неоднозначний провенанс.* Якщо довірена лінія/pristine базова лінія
недоступна: НЕОБСЛУГОВУВАНА (unattended) автоматична фільтрація
ПОВИННА fail closed. Стейджування усе ще може випустити dry-run/звіт, що
показує поле, ефективне/сире legacy-значення, статус провенансу,
причину неоднозначності й кандидатську дію — але не повинен мовчки
вирішувати «перевизначення проти shipped-default». Обов'язковий
безпечний шлях: оператор явно переглядає/підтверджує вибір поля →
кандидат перегенерується → звичайна валідація парсера/схеми/
безпеки/еквівалентності. Жоден неоднозначний непідтверджений кандидат
не може стати eligible для активації.

*Дрейф значення за замовчуванням між релізами.* Якщо стара доведена
поставлена база за замовчуванням відрізняється від цільового
значення за замовчуванням v2 — це повинно бути явно повідомлене як
delta міграції, а не мовчки вирішене на користь «закріпити старе
назавжди» чи «мовчки прийняти нову поведінку». Оператор-володіні/
доведені значення перевизначень повинні зберігатися точно; доведені
shipped-default значення зазвичай лишаються опущеними як
перевизначення; але розбіжність між старою поставленою базою й
цільовим значенням за замовчуванням v2 повинна бути покрита або
задокументованою продуктовою політикою міграції, або явним
підтвердженням/вибором оператора — до активації.

*Уточнення «ефективної еквівалентності».* Твердження про повну
«еквівалентність» стейджованого v2-кандидата ефективній legacy-
конфігурації уточнюється так: усі доведені/підтверджені
оператор-контрольовані значення вимагають точного семантичного
збереження; згенеровані/похідні значення повторно виводяться за
канонічними правилами v2, а не персистуються; shipped-default значення
підпорядковуються політиці провенансу/дрейфу за замовчуванням вище.
Якщо стара поставлена база за замовчуванням == цільова база за
замовчуванням v2 — опущення зберігає ефективну еквівалентність. Якщо
вони відрізняються — потрібна явна обробка delta міграції перед
активацією. Це усуває контраст, де shipped-defaults навмисно
опускаються, але водночас нібито кожне старе ефективне
shipped-default значення мало б завжди серіалізуватися/зберігатися.

*Метадані кандидата.* Неактивний стейджований кандидат/звіт міграції
повинен нести достатню ідентичність провенансу, щоб запобігти пізнішій
активації проти іншого джерела/базової лінії — концептуально:
ідентичність/хеш legacy-джерела, ідентичність провенансу/базової лінії
legacy, ідентичність цільової схеми/значень за замовчуванням,
ідентичність підтвердження оператора (якщо вимагається), ідентичність/
хеш кандидата (точні назви полів — деталь реалізації). Захист дрейфу
шляху активації повинен перевіряти, що ці ідентичності не зазнали
дрейфу. Не плутати це з журналом міграції, що використовується під
час активації.

**Активація (окрема, явно контрольована стадія — див. «Активація
міграції та відкат» нижче):**

- просуває валідованого проміжного кандидата в активну конфігурацію
  за ефективним активним основним шляхом (див. «Ефективний активний
  основний шлях» нижче);
- вимагає, щоб продакшн-завантажувач цільової моделі вже був
  присутній у запущеній інсталяції;
- є окремою, пізнішою, явно контрольованою операцією — сама
  конвертація не надає можливості активації;
- метадані проміжного кандидата фіксують достатньо ідентифікаційної
  інформації, щоб запобігти активації проти неправильної цілі, як
  мінімум: намір AUTO-проти-EXPLICIT `-ConfigPath`, точний розв'язаний
  ефективний основний шлях, ефективний каталог конфігурації, шлях
  site-файлу `BRAVO.local.config` за наявності,
  базову лінію джерела/хеш (або еквівалентну ідентичність дрейфу) та
  ідентичність/хеш кандидата — точні назви полів є деталлю реалізації,
  а не фіксуються цим документом.

### Ефективний активний основний шлях (AUTO проти EXPLICIT)

Міграція та активація повинні визначатися в термінах **ефективного
активного основного шляху конфігурації**, а не припущення, що
основний файл завжди `<ConfigRoot>\BRAVO.config`. Наявний контракт
AUTO/EXPLICIT для `-ConfigPath` (`BRAVO_SETUP.ps1`, визначення
завдань Task Scheduler, `BRAVO_CONFIGURATOR.ps1`) уже розрізняє два
режими, і міграція повинна зберігати цю відмінність:

**Режим AUTO** — оператор явно не вказав `-ConfigPath`:

```text
effective primary path = <ConfigRoot>\BRAVO.config
```

якщо основне перевизначення взагалі існує (воно залишається
опціональним).

**Режим EXPLICIT** — оператор явно вказав `-ConfigPath <шлях>`
(наприклад, `C:\Program Files\BRAVO-Toolkit\CONFIGS\SERVER1.config`): точний розв'язаний
шлях є наміром оператора й **повинен залишатися активним основним
шляхом протягом усієї міграції**. Він не повинен мовчки
перетворюватися на `<RuntimeRoot>\BRAVO.config` чи
`<ConfigRoot>\BRAVO.config`.

**Бажана канонічна модель активації — зберегти шлях, змінити
формат:**

```text
застарілі (legacy) дані/код за точним ефективним шляхом
    -> міграція ->
дані v2 за ТИМ САМИМ точним ефективним шляхом
```

Назва файлу/шлях не визначає legacy проти v2 — це робить визначення
формату/схеми (див. «Активний режим набору конфігурації» вище), а
продакшн-завантажувач приймає обидва стани. Тому активація зазвичай
**не повинна** вимагати перенацілення запланованих завдань чи
лаунчерів. Керівний принцип: **міграція змінює формат/вміст активної
конфігурації, а не явний намір оператора щодо основного шляху.**
Site-перевизначення слідує тому ж ефективному каталогу конфігурації, що
й сьогодні: для явного основного шляху
`C:\Program Files\BRAVO-Toolkit\CONFIGS\SERVER1.config` відповідний
`BRAVO.local.config` розв'язується з
`C:\Program Files\BRAVO-Toolkit\CONFIGS\BRAVO.local.config` — він не
переноситься мовчки в `RuntimeRoot`. Для режиму
AUTO/без-основного-файлу застосовується задокументована поведінка на
основі `ConfigRoot`. `RuntimeRoot` не обов'язково є `ConfigRoot`, а
`ConfigRoot` не обов'язково є каталогом, що мається на увазі місцем
розташування пакета; міграція повинна зберігати ефективне розташування
конфігурації.


Після активації кожен наявний персистентний споживач повинен і надалі
розв'язувати активну конфігурацію v2: точний `-ConfigPath`
запланованого завдання, точний `-ConfigPath` ручного лаунчера та
будь-який інший персистентний споживач залишаються незмінними,
оскільки той самий ефективний основний шлях тепер містить ДАНІ v2.
Якщо майбутня реалізація натомість обере перенацілення шляху, це
інший, ризикованіший дизайн, і вона ПОВИННА атомарно оновити кожного
персистентного споживача всередині захищеної відкатом транзакції
активації (див. «Активація міграції та відкат» нижче) — перенацілення
не є бажаним/дизайном за замовчуванням.

### Збереження при оновленні (updater preservation)

> **Реалізовано (#154, B6 / PR #185).** Нижче — контракт, який ця
> реалізація виконує і який лишається обов'язковим регресійним
> покриттям.

- оновлення/апгрейд toolkit на місці ніколи не повинне мовчки
  перезаписувати, знищувати чи пошкоджувати `BRAVO.local.config` — а
  також, протягом перехідного вікна, наявний на сервері legacy
  `BRAVO.config`. Обидва є шарами, що належать оператору, а не вмістом,
  що постачається пакетом, і пакет ніколи не скидає їх до заводських
  значень за замовчуванням під час оновлення;
- legacy `BRAVO.config` ніколи не повинен мовчки замінюватися пакетним
  файлом — оновлення не перезаписує наявний файл конфігурації сайту лише
  тому, що пакет постачає новий файл за замовчуванням.
  `deploy/Update-BRAVOServer.ps1` свідомо виключає його з копіювання;
- це повинно бути покрите явними регресійними випадками, а не лише
  задокументоване як очікування, включно як мінімум із:
  1. `BRAVO.local.config` переживає успішне оновлення без змін;
  2. `BRAVO.local.config` переживає невдале оновлення без змін;
  3. `BRAVO.local.config` переживає rollback без змін;
  4. legacy `BRAVO.config` ніколи мовчки не замінюється пакетним файлом.

Рутинні оновлення пакета на місці (не активація міграції) ніколи не
змінюють активний режим набору конфігурації; транзакція резервного
копіювання/відкату, описана в «Активація міграції та відкат» нижче,
стосується саме активації міграції, а не кожного оновлення.

### Активація міграції та відкат

> **Реалізовано оркестратором pilot (#208–#212).** Контракт нижче
> описує, що саме `deploy/Start-BRAVOConfigV2Pilot.ps1` має
> гарантувати: hash-bound approval, TOCTOU-gate, взаємне виключення
> мутуючих операцій над одним `InstallRoot`, довговічний журнал і
> rollback-first відновлення.
Активація просуває валідованого проміжного кандидата v2 (див.
«Міграція: конвертація проти активації» вище) у діючий набір
конфігурації, за ефективним активним основним шляхом (див.
«Ефективний активний основний шлях» вище) — вона не припускає, що
основний файл завжди `<ConfigRoot>\BRAVO.config`. Вона оперує повним
набором конфігурації як однією транзакцією, ніколи файл-за-файлом за
принципом best effort, і повинна переживати раптове переривання
(вбивство процесу, аварію хоста, перезавантаження, втрату живлення),
а не лише перехоплені в процесі збої.

**Синхронізація читання та активації (контракт блокування) — закриває
вікно TOCTOU.** Між попередньою перевіркою відновлення (recovery
preflight) та фактичним початком активації існує вікно стану гонки:
звичайний читач може пройти перевірку наявності незавершеного журналу,
а потім активація почне просування саме в той момент, коли читач
зчитує проміжний, наполовину просунутий стан. Сама наявність журналу
не є достатнім механізмом синхронізації для закриття цього вікна.
Явний контракт спільного читача / ексклюзивного writer'а обов'язковий:

- **звичайний завантажувач:** перед попередньою перевіркою
  відновлення та перед читанням/класифікацією активної конфігурації
  отримує спільну/read-side транзакційну блокування конфігурації
  (shared/read-side configuration transaction lock). Утримує це
  блокування безперервно протягом: перевірки стану відновлення,
  визначення формату, класифікації набору конфігурації, вилучення
  конфігурації, захоплення вхідних даних для мерджу. Звільняє
  блокування лише після отримання одного узгодженого незмінного
  знімку (snapshot) конфігурації;
- **активація/відновлення:** вимагає ексклюзивної/write-side
  блокування (exclusive/write-side lock), яка конфліктує з кожним
  звичайним читачем. Активація не повинна починати просування, доки
  будь-який уже наявний звичайний читач утримує спільне блокування.
  Жоден новий звичайний читач не може увійти, доки активація/
  відновлення володіє ексклюзивним блокуванням;
- це закриває гонку: попередня перевірка читача → активація
  починається → читач читає посеред просування;
- не покладатися лише на наявність журналу як на механізм
  синхронізації.

**Канонічний порядок отримання блокування** (щоб уникнути
гонок/дедлоків):

Звичайне завантаження:
1. отримати спільне/read блокування;
2. перевірити журнал міграції/стан відновлення;
3. якщо потрібне відновлення: звільнити/підвищити відповідно до
   канонічного шляху відновлення (відновлення отримує ексклюзивне
   володіння);
4. інакше прочитати/класифікувати/вилучити узгоджену конфігурацію;
5. звільнити спільне блокування.

Активація/відновлення:
1. отримати ексклюзивну транзакційну блокування;
2. перевірити володіння журналом/транзакцією;
3. виконати транзакцію/відновлення;
4. звільнити лише після досягнення стабільного узгодженого режиму.

Не вигадувати небезпечне підвищення блокування (lock upgrade), що
призводить до дедлоку — якщо підвищення не може бути атомарним:
звільнити спільне блокування, отримати ексклюзивне блокування,
повторно перевірити журнал/стан з нуля.

**Активація на основі перевірки шляху (виконується перед усім
іншим):** перед зміною будь-якого активного файлу активація
перевіряє:

- поточний ефективний основний шлях усе ще відповідає ефективному
  основному шляху, зафіксованому в цілі проміжної міграції;
- намір AUTO/EXPLICIT `-ConfigPath` не змінився з моменту
  стейджування;
- вихідна legacy-конфігурація не зазнала дрейфу з моменту генерації
  кандидата (збіг базової лінії/хешу);
- очікувана ідентичність локальної конфігурації не змінилася;
- кандидат належить саме цій цілі активації.

Будь-яка невідповідність **fail closed** — активація не
продовжується, і оператор повинен перегенерувати/повторно валідувати
кандидата.

**Довговічний write-ahead журнал активації.** Оскільки перехоплений
`try`/`catch`/`finally` не може захистити від завершення процесу,
аварії хоста, перезавантаження чи втрати живлення між мутаціями
файлів, активація використовує довговічний write-ahead журнал, що
зберігається поза файлами, які просуваються, у захищеному місці
рантайм/стану (точний постійний шлях є деталлю реалізації канонічного
кореня стану міграції, не фіксується цим документом). Журнал
концептуально фіксує: ID активації; ідентичність цільового
хоста/інсталяції; намір AUTO/EXPLICIT `-ConfigPath`; точний активний
основний шлях; стан активного локального шляху; ідентичність набору
резервної копії; ідентичність набору кандидата; поточну фазу/стан
активації; а також мітки часу/версію/схему за потреби. Точні назви
полів є деталлю реалізації.

**Контекст авторизованої перевірки власника активації (Fix B).**
Активація вже володіє ексклюзивною транзакційною блокуванням. Журнал
містить ID активації / ідентичність транзакції. Виклик верифікації
повинен нести ту саму ідентичність транзакції/здатність (capability),
що й активна транзакція. Завантажувач перевіряє: ексклюзивне
блокування належить саме цій транзакції; ID активації збігається з
власником журналу; фаза журналу явно дозволяє верифікацію власником.
Лише тоді верифікація, що належить активації, може інспектувати щойно
просунутий набір v2, не запускаючи rollback-first відновлення проти
самої себе. Звичайні процеси НЕ ПОВИННІ мати можливість запросити чи
обійти цей режим просто встановивши звичайний CLI-прапорець/змінну
середовища — це внутрішня довірена транзакційна можливість (trusted
transaction capability). Якщо володіння не може бути доведене: FAIL
CLOSED, без обхідних шляхів. Шлях верифікації власником НЕ ПОВИНЕН
послаблювати валідацію — він використовує ТОЙ САМИЙ парсер v2,
валідатор схеми, перевірку типів, класифікацію режиму, семантику
мерджу, інваріанти безпеки та перевірки ефективної конфігурації, що й
звичайне продакшн-завантаження. Лише попередня перевірка «неповний
власний журнал активації означає відкат» є транзакційно-обізнаною
(transaction-aware). Не створювати другу, слабшу реалізацію
валідації.

**Необхідний порядок транзакції:**

1. отримати ексклюзивне володіння транзакцією;
2. перевірити поточний активний набір legacy;
3. перевірити повний проміжний кандидат v2;
4. перевірка на дрейф шляху/наміру/джерела (activation path guard);
5. створити повну резервну копію активного набору та перевірити
   резервну копію;
6. довговічно створити write-ahead журнал;
7. просунути набір кандидата через write-ahead переходи фаз;
8. забезпечити узгоджений активний набір v2 без конфліктів;
9. авторизована власником, продакшн-еквівалентна перевірка (це
   контекст верифікації Fix B вище);
10. перевірка ефективної еквівалентності/безпеки/персистентних
    споживачів;
11. довговічно записати фазу `Committed`;
12. усиновлення (adoption) тепер підтверджене фактом `Committed`;
13. опціональні похідні метадані усиновлення можуть бути
    записані/узгоджені;
14. відкладене очищення згідно з політикою зберігання;
15. звільнити ексклюзивне володіння.

**Канонічне авторитетне правило стану підтвердження/усиновлення
(Fix C).** Довговічний журнал, що досягає фази `Committed`, є
авторитетним атомарним сигналом усиновлення (adoption) міграції.
Порядок: успішна верифікація → довговічно зберегти `Committed` →
похідно виставити/деривувати adoption=confirmed САМЕ З `Committed`.
Якщо існує окремий індекс/маркер усиновлення для операційного
пошуку, це похідний стан, а не авторитет, і він може бути
персистований лише ПІСЛЯ `Committed`; відновлення повинне узгоджувати
його з авторитетного журналу. Обов'язковий інваріант: якщо журнал !=
`Committed`, інсталяція НЕ є довговічно усиновленою. Аварія до
`Committed` ⇒ обробляється за точною розбивкою фаз нижче (не кожна
pre-commit невдача означає повне відновлення активного набору —
див. «Відкат до `Committed`» нижче); усиновлення не підтверджене,
доки `Committed` не досягнуто. Аварія після `Committed`, але до
оновлення похідного маркера ⇒ конфігурація залишається усиновленою,
похідний маркер відновлюється з `Committed`. Ніякого «розщеплення
мозку» (split-brain) не допускається.

**Відкат до `Committed` (pre-commit failure) — точна розбивка фаз.**
Неправильно трактувати БУДЬ-ЯКУ невдачу кроків 1–10 як таку, що
вимагає відновлення повного попереднього набору конфігурації:
відновлення активного набору можливе лише тоді, коли справді існує
верифіковане джерело відкату (резервна копія) І мутація активного
набору вже почалася чи могла початися. До того — немає що
відновлювати, і немає з чого. Розбивка за фазами:

- **PRE-MUTATION PREFLIGHT** (крок 1: отримання ексклюзивного
  володіння). Якщо ексклюзивне володіння транзакцією не отримано:
  НЕМАЄ володіння транзакцією, НЕМАЄ активної мутації, НЕМАЄ
  відкату — це FAIL/ABORT, а не подія відкату. Забороняється
  ініціювати відновлення чи відкат без володіння необхідним
  ексклюзивним транзакційним блокуванням. Невдача отримання
  блокування **не є** подією rollback;
- **PRE-MUTATION PREFLIGHT** (кроки 2–4: перевірка активного
  legacy-набору, перевірка проміжного кандидата v2, перевірка
  дрейфу шляху/наміру/джерела). До того, як існує верифікована
  резервна копія, і до будь-якої мутації активного файлу: FAIL
  CLOSED, ABORT ACTIVATION, активна конфігурація лишається
  НЕЗМІННОЮ, операція ВІДНОВЛЕННЯ НЕ виконується. Ексклюзивне
  володіння звільняється звичайним чином після досягнення
  стабільного стану невдачі. Не вигадувати фіктивну вимогу відкату
  для активного набору, який ніколи не змінювався;
- **BACKUP PREPARATION** (крок 5: створення й перевірка повної
  резервної копії). Резервна копія стає валідним джерелом відкату
  лише ПІСЛЯ того, як повний набір резервної копії створено І
  перевірено. Якщо створення чи перевірка резервної копії
  провалюється: активна конфігурація МАЄ лишатися незмінною;
  НЕПОВНА/НЕПЕРЕВІРЕНА резервна копія НІКОЛИ НЕ використовується як
  джерело відновлення. Активація переривається. Частковий артефакт
  резервної копії може бути збережений для діагностики чи безпечно
  очищений згідно з політикою реалізації, але він не є авторитетним
  матеріалом для відновлення;
- **JOURNAL PREPARATION** (крок 6: довговічне створення write-ahead
  журналу). Мутація активного набору не повинна починатися, доки не
  існують ОДНОЧАСНО: верифікована повна резервна копія І довговічний
  write-ahead журнал. Якщо створення журналу не може бути довговічно
  завершене: активна конфігурація МАЄ лишатися незмінною, ABORT,
  ВІДНОВЛЕННЯ АКТИВНОГО НАБОРУ НЕ ПОТРІБНЕ. Верифікована резервна
  копія може лишитися невикористаним артефактом відновлення, але
  активація не повинна продовжуватися;
- **MUTATION-МЕЖА.** Явна концептуальна межа: «МУТАЦІЯ НЕ
  РОЗПОЧАТА» проти «МУТАЦІЯ МОГЛА РОЗПОЧАТИСЯ». Довговічний журнал
  повинен містити достатньо інформації про фазу/стан, щоб відновлення
  могло розрізнити ці два випадки (точні назви фаз — деталь
  реалізації). Обов'язковий порядок: верифікована резервна копія →
  підготовлений довговічний журнал → стан журналу дозволяє мутацію →
  перша активна мутація. Ніколи навпаки: спочатку мутація, а потім
  виявлення, що резервна копія/журнал були недійсними;
- **ACTIVE MUTATION** (кроки 7–10: просування кандидата, узгоджений
  активний набір v2, авторизована власником перевірка, перевірка
  ефективної еквівалентності/персистентних споживачів). Лише коли
  ОДНОЧАСНО існують: верифіковане джерело відкату, довговічна
  ідентичність транзакції/журнал, і активна мутація вже почалася чи
  могла початися — застосовується rollback-first відновлення повного
  попереднього набору конфігурації. Для pre-`Committed` невдачі на
  цій фазі: відновити повний попередній набір під ексклюзивним
  володінням, використовуючи верифіковану резервну копію, пов'язану
  саме з цією транзакцією — ніколи частковий відкат, ніколи
  резервна копія з іншої транзакції, ніколи вгадана резервна копія.
  Відновлення на рівні набору відновлює один узгоджений попередній
  режим (наприклад, «legacy-основний + legacy-локальний»
  відновлюється разом, якщо це був стан до міграції), ніколи
  Відкат ніколи не повинен залишати активними одночасно два різні стани
  site-шару, окрім випадку, коли один з них перебуває лише в явно
  неактивній зоні резервної копії/стейджування. Транзакція лишається НЕ
  усиновленою.


**Відновлення після аварії/перезавантаження для pre-commit фаз.**
Відновлення повинне fail closed, якщо не може довести: ідентичність
транзакції; ексклюзивне володіння; ідентичність верифікованої
резервної копії; стан журналу; зв'язок між резервною копією й
транзакцією. Якщо журнал вказує, що мутація могла початися, але
пов'язана з нею верифікована резервна копія відсутня/пошкоджена/
неперевірювана: FAIL CLOSED, зберегти наявні докази, вимагати
оператора/задокументований шлях відновлення — НІКОЛИ не вгадувати
іншу резервну копію, не продовжувати звичайне завантаження, не
виконувати best-effort частковий відкат. Якщо стан довговічного
журналу доводить, що активна мутація ніколи не починалася —
відновлення активного набору НЕ потрібне; відновлення може
завершити/очистити покинуту підготовлену транзакцію згідно з
задокументованою політикою.

**`Committed` — точка неповернення для усиновлення (post-commit
failure).** Щойно фаза `Committed` довговічно записана (крок 11),
БЕЗ ВІДКОТУ ДО LEGACY. Це застосовується до будь-якої невдачі ПІСЛЯ
цієї точки, включно з:

- відновленням/ремонтом похідного підтвердження чи маркера
  усиновлення;
- опціональним узгодженням похідних метаданих усиновлення (крок 13);
- відкладеним очищенням (крок 14);
- очищенням legacy-резервної копії;
- іншим явно post-commit господарським очищенням.

Жодна з цих невдач НЕ повинна відновлювати старий (legacy) набір
конфігурації. Натомість:

- активна v2-конфігурація лишається авторитетною;
- журнал лишається `Committed`;
- невдача звітується як відновлювана (repairable) post-commit
  помилка метаданих/очищення, а не як провал транзакції усиновлення;
- похідний маркер може бути пересозданий/відремонтований з
  `Committed`;
- очищення є ідемпотентно повторюваним під час пізнішого
  обслуговування/відновлення;
- звичайне відновлення НІКОЛИ не повинне робити висновок «не
  усиновлено» з відсутності чи невдачі похідних метаданих, коли
  журнал перебуває у стані `Committed`.

Це узгоджується з інваріантом Fix C вище ("якщо журнал != `Committed`,
інсталяція НЕ усиновлена" має зворотний бік: коли журнал ==
`Committed`, інсталяція Є усиновленою, і жодна post-commit
господарська невдача цього не скасовує).

**Відновлення після аварії/перезавантаження незалежне від пам'яті
процесу.** Відновлення повинне коректно працювати з абсолютно нового
процесу й не повинне залежати від `catch`/`finally`, змінних у
пам'яті чи того, що оригінальний процес PowerShell усе ще працює.
Перш ніж звичайний шлях класифікації режиму конфігурації/мерджу
споживає активні файли, попередня перевірка відновлення міграції
перевіряє наявність незавершеного журналу активації; якщо такий
існує, першим запускається відновлення — звичайний завантажувач
ніколи не повинен просто натрапити на наполовину просунуту змішану
конфігурацію й трактувати це як звичайну помилку оператора. Політика
— **rollback-first**: для будь-якої незавершеної/незафіксованої
активації повний попередній активний набір конфігурації відновлюється
з перевіреної резервної копії, після чого відновлений набір повторно
валідується як один узгоджений підтримуваний режим (див. «Активний
режим набору конфігурації» вище). Лише після завершення відновлення
може продовжуватися звичайне завантаження.

**Невдача відновлення fail closed.** Якщо відновлення не може
безпечно відновити попередній повний набір конфігурації, воно fail
closed: не вгадує пріоритет, не видаляє журнал чи резервні копії, не
продовжує частково, показує конкретну помилку відновлення міграції
та зберігає криміналістичні докази/докази для відновлення.

**Єдиний writer (single writer).** Активація та відновлення не
повинні виконуватися одночасно. Одна інсталяція може мати щонайбільше
одну активну транзакцію міграції одночасно, використовуючи
канонічний загальномашинний підхід блокування/стану репозиторію, коли
той реалізований; якщо інша активація/відновлення вже володіє
транзакцією, нова fail closed як «зайнято», а не починає другу
міграцію. Цей контракт єдиного writer'а доповнюється явним контрактом
блокування читач/writer вище (Fix A): звичайні читачі можуть
співіснувати між собою через спільне блокування, але жоден читач не
може співіснувати з ексклюзивним writer'ом активації/відновлення.

**Відновлення ідемпотентне.** Відновлення повинне бути безпечним для
повторного запуску — якщо саме відновлення перерване, наступний
виклик відновлення повинен усе одно безпечно збігтися, і жодна
повторна спроба відновлення не може пошкодити останню перевірену
резервну копію.

### Регресійні вимоги / вимоги Definition-of-Done (B7)

Перш ніж Configuration v2 можна позначити завершеним у `ROADMAP.md`,
повинні існувати й проходити перелічені нижче перевірки — **на
ФІНАЛЬНОМУ шляху завантаження**, а не на helper-ах в обхід нього.

**Parser / non-execution** (site-шар, `BRAVO.local.config`):

- виклик cmdlet → FAIL;
- виклик функції → FAIL;
- виклик методу / доступ до члена → FAIL;
- довільне звернення до змінної (`$foo`, `$env:`, `$global:`,
  `$script:`) → FAIL;
- підвираз `$( ... )` → FAIL;
- арифметичний/діапазонний вираз → FAIL;
- `$true` / `$false` / `$null` → PASS як ЛІТЕРАЛИ (не як змінні);
- `$global:true` → FAIL (лише СХОЖИЙ на дозволену константу);
- дубльований ключ → FAIL;
- malformed AST → FAIL;
- **доказ non-invocation**: шкідливий або пошкоджений site-файл не може
  спричинити виконання коду, навіть якщо містить синтаксично
  правдоподібний PowerShell поза межами літеральних даних — тобто
  доведення властивості 4 вимоги до безпечного парсера, а не лише того,
  що невалідний синтаксис відхиляється.

**Схема:**

- коректні скалярні типи → PASS;
- неправильний скалярний тип → FAIL;
- nullable-поле зі значенням `$null` → PASS (згідно з політикою поля);
- non-nullable поле зі значенням `$null` → FAIL;
- невідомий top-level блок → FAIL;
- невідомий БАТЬКІВСЬКИЙ вузол → FAIL;
- невідомий КІНЦЕВИЙ сегмент (leaf) → PASS + видимі метадані/попередження
  згідно з рішенням D3;
- непідтримуваний/невідомий `configSchemaVersion` → FAIL;
- файл без маркера версії → PASS як версія 1 + попередження + метадані.

**Мердж:**

- скалярне перевизначення;
- вкладене перевизначення;
- масив повністю ЗАМІНЮЄ масив за замовчуванням (а не мерджиться
  поелементно);
- явний порожній масив `@()` — валідне, навмисне перевизначення;
- значення за замовчуванням `maintenanceSettings.Limits.ExcludedDrives`
  лишається `@()`;
- семантика словників;
- нормалізація числових типів там, де вона задумана;
- жодного приховування дрейфу типів через coercion PowerShell.

Базова семантика мерджу вже існує й уже протестована для сьогоднішнього
мердж-рушія Foundation — ці пункти про повторне доведення, що вона
переживає фінальний шлях завантаження, а не про винайдення її заново.

**Деривація:**

- доказ, що похідні обчислення (`Resolve-BRAVOConfigurationDerivation`)
  виконуються ПІСЛЯ мерджу, а не до нього.

**Безпека:**

- послаблення критичного для безпеки параметра → fail closed у режимі
  `Enforce`;
- задокументована семантика `Warn`
  (`BRAVO_RUNTIME_INTEGRITY_MODE=Warn`);
- задокументована поведінка явного перевизначення
  (`BRAVO_ALLOW_WEAKENED_SECURITY=1`);
- секрети не можуть опинитися у конфігурації чи в evidence.

**Намір (`-ConfigPath`) — матриця AUTO проти EXPLICIT:**

- AUTO + основний файл відсутній + site-файл відсутній ⇒ підтримується,
  лише built-in;
- AUTO + основний файл відсутній + site-файл присутній ⇒ підтримується,
  `DEFAULT < BRAVO.local.config`;
- AUTO + legacy основний файл присутній ⇒ підтримується, перехідний
  режим;
- EXPLICIT + основний файл існує ⇒ підтримується;
- EXPLICIT + основний файл відсутній + site-файл відсутній ⇒ FAIL
  CLOSED;
- EXPLICIT + основний файл відсутній + site-файл присутній ⇒ FAIL
  CLOSED.

**Стан інсталяції:**

- інсталяція лише з дефолтів;
- мігрована інсталяція (site-значення в `BRAVO.local.config`);
- неповна міграція (значення лишились у legacy `BRAVO.config`) —
  видима через
  `BravoConfigurationMetadata.PrimaryConfigOverridesCanonicalDefaults`;
- неоднозначне усиновлення;
- розбіжність стану.

**Еквівалентність legacy проти цільового стану:**

- щонайменше одна нетривіальна контрольна фікстура, де
  `EffectiveConfig(BEFORE) == EffectiveConfig(AFTER)` з урахуванням
  типів значень.

**Збереження при оновленні:**

- `BRAVO.local.config` переживає успішне оновлення;
- `BRAVO.local.config` переживає невдале оновлення;
- `BRAVO.local.config` переживає rollback;
- legacy `BRAVO.config` ніколи мовчки не замінюється пакетним файлом.

**Покриття контракту провенансу міграції** (див. «Контракт провенансу
перед фільтрацією legacy-полів» вище):
  - відома точна pristine база + поле відрізняється ⇒ поле
    зберігається як локальне відхилення;
  - відома точна pristine база + поле незмінне ⇒ поле опускається як
    shipped-default;
  - незмінне базове значення + довірений явний pin оператора ⇒ поле
    зберігається;
  - згенероване/виявлене legacy-значення ⇒ не персистується;
  - лінія джерела legacy невідома ⇒ неконтрольована (unattended)
    міграція заблокована як неоднозначна;
  - legacy-конфіг персистований через кілька оновлень + доступна лише
    поточна база пакета ⇒ поточна база НЕ повинна вважатися
    провенансом;
  - старий shipped-default == цільовий v2-default ⇒ опущення зберігає
    ефективне значення;
  - старий shipped-default != цільовий v2-default ⇒ вимагається явна
    обробка delta міграції;
  - підтверджене оператором неоднозначне поле ⇒ кандидат може бути
    перегенерований і провалідований;
  - джерело/базова лінія змінюються після стейджування ⇒ активація
    відхиляється захистом дрейфу;
**Покриття стейджування/активації міграції:**

- стадія стейджування залишає кожен живий файл конфігурації побайтово
  незмінним;
- проміжний кандидат не виявляється продакшн-завантажувачем;
- активація просуває повний, узгоджений набір;
- невдача активації відновлює повний попередній набір;
- відкат не може залишити активними два різні стани site-шару
  одночасно;
- недійсний site-файл ніколи не відкочується до legacy-виконання —
  повторно перевірено на рівні набору конфігурації, а не лише на рівні
  одного файлу;
- міграція з явним зовнішнім `-ConfigPath` (EXPLICIT) зберігає точний
  шлях: командний рядок запланованого завдання, що містить
  `-ConfigPath "<explicit path>"`, незмінний після успішної активації;
  ручний лаунчер, що використовує явний ефективний шлях конфігурації,
  залишається валідним; site-перевизначення розв'язується поряд з
  ефективною конфігурацією;
- активація відхиляє проміжного кандидата, коли ефективний
  `-ConfigPath` чи базова лінія джерела змінилися після стейджування
  (activation path guard вище);
- режим AUTO залишається канонічним і не порушується захистом
  EXPLICIT-шляху;
  - покриття синхронізації читача/writer'а та відновлення TOCTOU
    (Fix A): звичайний читач, що отримав спільне блокування до старту
    активації, не бачить наполовину просунутого стану; активація не
    починає просування, доки не звільниться будь-яке наявне спільне
    блокування; жоден новий читач не входить, доки триває активація/
    відновлення;
  - покриття верифікації власника активації (Fix B): неавторизований
    виклик без дійсної ідентичності транзакції відхиляється fail
    closed; авторизована власником верифікація використовує ту саму
    реалізацію валідації, що й звичайне завантаження, і не запускає
    rollback-first відновлення проти власної щойно просунутої
    транзакції;
  - покриття авторитетного стану `Committed`/усиновлення (Fix C):
    аварія до `Committed` залишає усиновлення непідтвердженим і
    відновлює legacy; аварія після `Committed`, але до оновлення
    похідного маркера, залишає конфігурацію усиновленою й відновлює
    похідний маркер з журналу;
  - покриття переривання/відновлення після аварії: раптове
    переривання, ін'єктоване (1) після створення журналу, але до
    будь-якої мутації активного файлу, (2) після просування основного
    файлу, але до просування локального, (3) під час або після
    просування локального v2, (4) до/після виведення з експлуатації
    legacy-локального, (5) після всіх просувань файлів, але до
    верифікації v2, (6) після верифікації, але до маркера `Committed`,
    і (7) під час оновлення самого довговічного журналу — після
    симульованого перезапуску/відновлення кожен випадок повинен
    довести: відновлення не залежить від пам'яті старого процесу;
    відновлюється (чи, для випадку (1), лишається незміненим — див.
    застереження нижче) один узгоджений підтримуваний режим
    конфігурації; жоден змішаний режим legacy/v2 не стає видимим для
    звичайного завантаження; явний ефективний `-ConfigPath`
    зберігається; заплановані/персистентні споживачі все ще
    націлені на валідну конфігурацію; резервна копія/журнал
    залишаються, якщо відновлення зазнає невдачі; а успішне
    відновлення ідемпотентне. **Застереження для випадку (1)** (узгоджено
    з «Відкат до `Committed` — точна розбивка фаз» нижче): якщо
    відновлення доводить із довговічного журналу, що жодна активна
    мутація файлу не почалася, воно НЕ відновлює/не змінює активні
    файли — коєрентний активний набір, що вже існував до спроби
    активації, лишається недоторканим як є. Це не суперечність із
    «відновлюється узгоджений режим» вище: властивість задовольняється
    тривіально, оскільки набір, що лишається недоторканим, сам є тим
    єдиним узгодженим підтримуваним режимом. Відновлення/заміна файлів
    із резервної копії застосовується лише до випадків (2)–(7), де
    мутація активного файлу вже почалася чи могла початися;
  - ідемпотентність відновлення: саме відновлення перерване, і
    наступний виклик відновлення все одно безпечно збігається без
    пошкодження останньої перевіреної резервної копії;
- покриття точної розбивки фаз відкату (див. «Відкат до `Committed`
  — точна розбивка фаз» вище):
  - крок 1 (отримання ексклюзивного володіння) провалюється ⇒ ABORT
    без спроби відкату — це НЕ подія rollback;
  - крок 2/3/4 (preflight-перевірки) провалюється ⇒ активний набір
    лишається незмінним, ВІДНОВЛЕННЯ НЕ виконується;
  - створення резервної копії провалюється ⇒ активний набір
    незмінний, неповна резервна копія ніколи не використовується як
    джерело відновлення;
  - перевірка резервної копії провалюється ⇒ активний набір
    незмінний, неперевірена резервна копія ніколи не використовується
    як джерело відновлення;
  - створення довговічного журналу провалюється до початку активної
    мутації ⇒ ABORT, відновлення активного набору НЕ потрібне;
  - аварія після довговічного створення журналу, але до активної
    мутації ⇒ відновлення доводить, що мутації не було, і не вимагає
    відновлення активних файлів;
  - невдача/аварія після першої активної мутації, але до `Committed`
    ⇒ повний попередній набір відновлюється з верифікованої резервної
    копії саме цієї транзакції;
  - журнал mutation-фази посилається на відсутню/пошкоджену резервну
    копію ⇒ fail closed, жодної вгаданої резервної копії;
  - невдача після `Committed` ⇒ без відкату до legacy (незмінний
    інваріант Fix C вище);
- прийняття збереження при оновленні (не вимагає real-server
  acceptance для того, щоб бути задокументованим як «done» на рівні
  кодової регресії, але real-server acceptance вимагається до того,
  як це буде описано як доведене в продакшні, згідно з політикою
  валідації репозиторію).

### Рекомендований розподіл PR — ІСТОРИЧНЕ, СКАСОВАНО

> **⚠️ ЦЕ НЕ ЧИННА ВИМОГА.** Попередня редакція документа містила
> детальний план із п'яти PR (A–E), побудований на **скасованій**
> архітектурі: DATA-only `BRAVO.config` як цільовий primary-шар,
> окремий машинно-локальний файл `BRAVO.config.local`, двоформатний
> зчитувач і остаточний v2-only cutover з видаленням
> legacy-завантажувача. Усі чотири передумови скасовані рішенням
> власника D2 (PR #173) і рішенням про відмову від `BRAVO.config`
> (#154, B4 частина 2), тому план видалено, а не позначено зноскою:
> залишений текст читався б як чинна вимога реалізувати скасовану
> архітектуру.
>
> Історію плану видно в git (`docs/design/BRAVO_CONFIGURATION_V2_COMPLETION.md`
> до цього коміту) та в PR #173, який уперше позначив ці секції
> застарілими.

**Фактично виконаний розподіл** (замінює той план):

| Етап | Що зроблено | PR |
| --- | --- | --- |
| A1–A4 | вирівнювання опису leaf, видимість невідомого leaf, симетрія суворості primary-шару, стійкість parity harness | #172, #165, #170, #171 |
| D2, D3 | рішення власника зафіксовані | #173 |
| B0 | інструмент дельти site-конфігурації | #169 |
| B1 | невиконуючий AST-парсер site-файлу (замінив `CheckRestrictedLanguage` + `& $scriptBlock`) | #174 |
| B2 | формальна схема типів, fail-closed | #183 |
| B3 | маркер `configSchemaVersion` і диспетч за версією | #184 |
| B6 | збереження site-файлу при оновленні | #185 |
| B4 ч.1 | legacy `BRAVO.config` перестав бути мовчазним | #186 |
| CI | parity harness перестав бути ручним | #193 |
| Pilot | артефакт, оркестратор, фікси реальних VM-прогонів | #207–#212 |

**Що лишилося** — B5 (міграція парку), B4 частина 2 (прибирання
`BRAVO.config` з пакета), B7 (DoD matrix + parity як required check).
Кожен етап — окремий вузький PR; порядок жорсткий і описаний у #154.
## Фінальний цільовий контракт (зведення)

```text
Вбудовані значення за замовчуванням є повними (частина пакета, а не файл).
BRAVO.local.config — опціональне DATA-only перевизначення рівня сайту.
DEFAULT < BRAVO.local.config
BRAVO.config — legacy; прибирається з пакета, не конвертується у v2.
BRAVO.config.local — скасовано (D2); не існує і не існуватиме.
Файл конфігурації є ДАНИМИ, а не КОДОМ.
Продакшн-рантайм ніколи не викликає BRAVO.local.config як код.
Невідомий top-level блок і невідомий батьківський вузол -> FAIL CLOSED.
Невідомий КІНЦЕВИЙ сегмент -> ПРИЙМАЄТЬСЯ + попередження + метадані (D3).
Некоректні типи не проходять валідацію.
Limits.ExcludedDrives default = @()
Масиви замінюють, а не мерджуються.
Явний @() є валідним перевизначенням.
Похідні обчислення рантайму відбуваються після сирого мерджу.
Секрети залишаються поза конфігурацією (Windows Credential Manager).
```

З цього контракту сьогодні в продакшні **вже виконується**:

- вбудовані значення за замовчуванням повні;
- `BRAVO.local.config` опціональний і читається як ДАНІ — невиконуючим
  AST-парсером, без створення й виклику scriptblock (#154, B1);
- невідомий top-level блок і невідомий батьківський вузол fail-closed;
  невідомий leaf приймається з попередженням і метаданими (#154, A2/D3);
- перевірка типів site-шару через формальну схему (#154, B2);
- диспетч за `configSchemaVersion` site-файлу (#154, B3);
- похідні обчислення рантайму виконуються після сирого мерджу;
- секрети залишаються поза конфігурацією (Windows Credential Manager);
- значення за замовчуванням `Limits.ExcludedDrives` дорівнює `@()`;
- масиви замінюють, а не мерджуються;
- явний `@()` є валідним перевизначенням;
- `BRAVO.local.config` переживає оновлення (#154, B6);
- застарілий `BRAVO.config` більше не мовчазний (#154, B4 ч.1);
- інструментарій міграції існує й перевірений реальними прогонами
  (#207–#212).

**Що залишається лише цільовим** (ще не є правдою в продакшні):

- site-значення парку живуть у `BRAVO.local.config`, а не в
  `BRAVO.config` — потребує **B5**;
- `BRAVO.config` відсутній у пакеті, і рантайм не вимагає його —
  потребує **B4, частини 2**;
- повна регресійна матриця Definition-of-Done, прогнана саме проти
  фінального шляху завантаження, і parity harness як required status
  check — потребує **B7**;
- рішення щодо `configSchemaVersion = 2` для site-маркера — предмет
  **B7**.

**Config v2 НЕ можна позначати завершеним**, доки B5, B4 частина 2 і B7
не завершені фактично, з доказами.

## Див. також

- `docs/design/BRAVO_CONFIGURATION_FOUNDATION_DESIGN.md` — історичний
  архітектурний рубіж для завершеної роботи P0 Foundation.
- `TODO_FEATURES.md`, FEAT-001 — оригінальний запит на функціонал і
  детальні нотатки бекложа.
- `ROADMAP.md`, P3.1 — канонічний поточний пріоритет/статус.
- `CHANGELOG.md`, "Не випущено (developer)" — запис фактично
  реалізованого для P0 Foundation, включно з відхиленнями від
  початкового дизайну.
- Issue #154 — виконавча обгортка епіка; при розбіжності канонічною є
  вона разом з останніми явними рішеннями власника.
- `docs/BRAVO_CONFIG_V2_PILOT_RUNBOOK.md` і
  `docs/BRAVO_CONFIG_V2_PILOT_MIGRATION_RUNBOOK_20260916.md` — фактичні
  операторські runbook-и pilot-міграції.
- `RELEASE_POLICY.md` §14.4 — parity harness як обов'язковий доказ для
  змін конфігураційного пайплайна.
